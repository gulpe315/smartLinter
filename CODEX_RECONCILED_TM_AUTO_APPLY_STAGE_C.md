1. 검증은 맞습니다. agy의 질문 2안은 원본 `h_i.start`가 적용 전 좌표라는 점을 놓쳤습니다. 길이가 변한 왼쪽 hunk가 있으면 적용 후의 오른쪽 hunk 위치는 달라집니다. 따라서 `oldText/newText`만 바꾸고 기존 시작 좌표로 역치환하는 것은 일반적으로 안전하지 않습니다.

정확한 역치환 좌표는 다음과 같습니다.

```text
postStart(i) = start_i + Σ(delta_j), j < i
delta_j = length(newText_j) - length(oldText_j)
```

이 점은 agy의 개별 되돌리기 논리와 동일합니다. 즉 질문 2 답변은 질문 3의 논리와 실제로 모순됩니다. 단, 모든 hunk의 길이 변화가 0이거나 hunk가 하나뿐인 특수 경우에는 우연히 문제가 드러나지 않습니다.

2. 최종 권장안은 Codex의 "전체 텍스트 재diff" 방식입니다.

```ts
revertHunks = extractDiffHunks(currentExpectedText, beforeText)
```

이 방식이 더 안전하고 단순합니다.

- 적용 후 텍스트 좌표계에서 직접 hunk를 만들므로 드리프트 보정이 필요 없습니다.
- 원본 hunk의 분할 방식·저장 상태에 덜 의존합니다.
- `validateHunks`, 역순 적용 검증, 결과 텍스트 및 `baseHash` 일치 검증을 한 흐름으로 처리할 수 있습니다.
- 정확한 `currentExpectedText`와 해시 일치를 선행 조건으로 삼으면, 재diff의 모호성은 되돌리기 결과의 정확성 문제가 아닙니다. hunk 분할이 달라져도 최종 문단이 `beforeText`인지가 핵심입니다.

드리프트 보정 절충안도 기술적으로는 가능합니다. 각 원본 forward hunk에 위 `postStart(i)`를 적용해 역 hunk를 만들고, 보정된 `start` 기준으로 오른쪽에서 왼쪽으로 적용하면 됩니다. 다만 구현·검증·유지보수 면에서 재diff보다 복잡하며, 이점은 "원래 항목별 hunk 정체성을 반드시 보존해야 한다"는 요구가 있을 때뿐입니다.

3. 개별과 일괄 되돌리기는 의미적으로 일관되게 맞물릴 수 있지만, 반드시 같은 hunk 생성 방식을 공유할 필요는 없습니다.

- 개별 되돌리기: 특정 항목의 번역문 위치를 찾아야 하므로, 활성 상태인 왼쪽 항목들의 길이 변화만 합산하는 동적 오프셋 계산(또는 동등한 앵커/재탐색)이 필요합니다.
- 일괄 되돌리기: 목표가 문단 전체를 `beforeText`로 정확히 복원하는 것이므로, `currentExpectedText → beforeText` 전체 재diff가 가장 직접적입니다.

일괄을 개별 되돌리기의 순차 실행으로 통일하는 것도 가능합니다. 단, 오른쪽 항목부터 왼쪽 항목 순서로 실행해야 합니다. 그러면 아직 남아 있는 왼쪽 항목들이 만드는 `postStart` 보정값이 유지되고, 오른쪽 항목의 되돌리기는 왼쪽 좌표에 영향을 주지 않습니다. 각 단계에서 현재 스냅샷·해시를 다시 검증해야 하며, 중간 실패 시 부분 되돌림이 생길 수 있습니다.

따라서 권장 구조는 다음입니다.

- 개별 Undo: 항목 단위 동적 오프셋/앵커 기반.
- Batch Undo: 하나의 원자적 명령으로 전체 재diff 기반.
- 공통: live snapshot, 기대 텍스트·해시 검증, 역순 hunk 적용, 결과 해시 검증.

이렇게 하면 사용자 의미는 일관되고, 배치 작업은 원자성 및 구현 단순성도 확보합니다.
