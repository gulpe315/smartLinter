# CODEX_RECONCILED_TRANSLATION_MODE_T5.md

## 재조율 결론

| 쟁점 | 최종 결론 |
|---|---|
| `tool-id="SmartLinter"` | 필수 조건으로 삼지 않는다. 있으면 출처 정보로 표시하되, 수용 여부는 `segmentId` + `<source>` 이중 검증으로 결정한다. |
| import 직전 전체 재스캔 | 필수 선행 조건으로 둔다. 성공한 최신 전체 스캔 결과에 대해서만 import 검증·병합을 수행한다. |
| 사용자 편집본 vs 외부 빈 target | 충돌로 표시하고 세그먼트별 선택을 요구한다. 자동 보존·무시는 하지 않는다. |

## 1. `tool-id`는 정보성 출처 마커

XLIFF 1.2에서 `<header>`/`<tool>`은 선택 사항이라 다른 CAT가 헤더를
생략·재작성한 파일을 거부할 근거가 되지 않는다. memoQ의 XLIFF 상호운용
문서도 세그먼트 분할/병합 시 ID 형식이 바뀔 수 있음을 명시 —
"CAT 왕복" 자체는 정상 시나리오이고, ID가 실제로 변했다면 완전 일치
계약 아래 자동으로 적용 안 되는 것으로 이미 충분히 막힌다. 헤더는
위조하기 쉬워 보안 경계로 부적절 — 실제 무결성은 `segmentId`+
`<source>` 이중 검증이 담당.

## 2. import 직전 성공한 전체 재스캔 필수

저장된 세션의 `sourceHash`는 과거 스냅샷일 뿐, 앱 종료 사이 에디터에서
원문이 바뀌지 않았다는 보장을 주지 않는다. 확정 흐름: 파일 선택 →
XML 사전 검증(세션 무변경) → 전체 재스캔 실행 → 성공 시
`mergeScannedParagraphs`로 세션 갱신 → 갱신된 세션 기준으로 매칭·충돌
분류 → 충돌 해결 후 단일 커밋. 재스캔 실패/타임아웃/취소 시 import
전체 중단, 세션 무변경. InDesign 미배치 story 발견 시 "기본 범위만
진행/포함 재스캔 후 진행/취소" 중 명시적 선택 필요.

## 3. `isUserEdited: true` + 명시적 빈 target = 충돌

자동으로 로컬 값을 보존하면 데이터 손실은 막지만, 외부 검토자가
의도적으로 비우거나 재작업 대상으로 돌려준 사실을 사용자에게 숨긴다.
T2의 `needs-validation` export 전체 차단, T3의 모호한 매칭 금지와
같은 맥락 — 안전성과 함께 판단 근거의 가시성을 우선한다. 단,
`<target>` 요소 자체가 없는 경우(번역 미제공)와 명시적으로 빈
`<target></target>`(의도적 삭제/재작업 요청)는 파서 단계에서 반드시
구분해야 한다 — 전자는 no-op+통계, 후자만 충돌 규칙 적용.
