# 완료 QA 카드 아카이브/재확인 제안 검토

## 결론

제안은 유효하며, **완료 카드를 활성 목록에서는 숨기되 별도 완료 이력으로 보존하고 필요할 때 다시 확인하는 방식**이 적절합니다. 다만 이는 단순 UI 토글이 아니라, 완료 당시의 스냅샷과 현재 문단의 상태를 명확히 구분하는 기능이어야 합니다. 특히 "다시 수정"은 과거 제안을 그대로 재실행하는 동작이 아니라, 대상 문단을 다시 찾고 최신 본문·해시를 확인한 뒤 안전하게 재검토하는 흐름으로 설계해야 합니다.

현재 작업 트리에는 제안에서 말한 `completedCards`와 사실상 같은 `appliedCards`가 이미 있습니다. `SUCCESS` 처리 시 카드를 `cards`에서 제거해 `appliedCards`로 옮기므로 활성 목록에는 보이지 않습니다. 따라서 새 배열을 또 만들기보다는 이 모델을 완료 아카이브로 공식화하고, 표시·재확인·보존 정책을 보완하는 편이 안전합니다. (`src/stores/qaStore.ts`, `src/services/rollback_guard.ts`)

## 1. 상태 저장 방식

`cards`는 작업 가능한 활성 카드만, `appliedCards`는 성공적으로 적용된 완료 아카이브만 보관하는 분리가 적합합니다. 기존 `dismissedCards`와도 의미가 다릅니다. 무시는 사용자가 제안을 채택하지 않기로 한 기록이고, 완료는 실제 문서 변경이 성공한 작업 이력입니다.

기존 `cards`에 `status: 'applied'`만 남겨 필터링하는 방식은 추천하지 않습니다. 활성 카드의 중복 제거, 새 QA 리포트의 권위 처리(`addReport`), 배치 무시, 카운터와 필터가 모두 활성 목록을 전제로 하므로 완료 항목이 섞이면 각 경로에서 누락·중복·잘못된 카운트가 생기기 쉽습니다. 이미 `appliedCards`로 이동하는 전이가 있으므로 이를 유지하는 것이 변경 범위도 작습니다.

아카이브에는 최소한 적용 시각, 적용 직후 해시, 당시 문단/제안 스냅샷, 적용 결과의 commandId를 남겨야 합니다. 현재 카드 정보만으로는 "언제 어떤 문서 상태에서 적용됐는가"를 충분히 설명하기 어렵습니다. 세션 종료 뒤에도 보이기를 기대하는지 또한 제품 결정이 필요합니다. 현재 스토어는 메모리 기반이므로 영속화 요구가 포함된다면 별도 저장소·보존 기간·문서별 격리 정책을 먼저 정해야 합니다.

## 2. 완료 카드의 재수정

완료 카드를 활성 카드로 단순 복귀시켜 과거 `originalSegment`/`suggestedSegment`를 바로 적용하는 것은 안전하지 않습니다. 적용 후 사용자가 같은 문단을 편집했거나, 해당 문자열이 여러 번 나타나거나, 문단 구조가 바뀌었을 수 있습니다. 과거 `paragraphHash`는 현재 문단의 base hash로 쓸 수 없습니다.

권장 흐름은 다음과 같습니다.

1. 완료 카드는 우선 읽기 전용 이력으로 연다.
2. "현재 위치 보기" 또는 "재확인"에서 `paragraphId`로 현재 문단을 조회한다.
3. 최신 본문과 해시를 얻은 뒤 과거 이슈가 아직 동일 위치·동일 문맥에 존재하는지 검증한다.
4. 정확히 검증되면 새 활성 카드(새 카드 ID, 최신 본문/해시)를 생성한다. 불일치하거나 애매하면 해당 문단의 micro QA 재분석으로 새 제안을 만든다.

즉, 과거 카드는 감사용 원본으로 보존하고 재수정은 새 작업 단위로 생성해야 합니다. 재검증 없이 원래 제안을 재사용할 수 있는 예외는 최신 본문이 적용 직후 스냅샷과 동일하고, 대상 구간을 단일하게 확인할 수 있을 때뿐입니다. 그 경우에도 새 `ReplacementCommand`와 최신 `baseHash`를 사용해야 합니다.

## 3. InDesign 문단 위치 추적과의 연결

Task B의 `command.paragraphId` 기반 문단 탐색은 이 기능의 선행 조건입니다. 완료 카드의 "위치 보기"는 치환 명령을 보내는 대신, 같은 식별자로 문단을 찾아 InDesign 문서를 활성화하고 해당 문단 또는 텍스트 범위를 선택하는 전용 브리지 명령이어야 합니다. 현재 치환 경로만으로는 안전한 위치 이동 API가 생기지 않습니다.

또한 조회와 이동은 같은 문서/세션을 검증해야 합니다. 현재 `indesign-para-{storyId}-{paragraphIndex}`는 문서 편집에 따라 paragraph index가 이동할 수 있으므로, 오래 보관한 카드에서 잘못된 문단을 가리킬 위험을 명시적으로 처리해야 합니다. 적어도 문서 식별자와 최신 문단 해시를 함께 확인하고, 찾지 못했거나 불일치하면 "위치를 찾을 수 없음—재분석 필요"로 실패해야 합니다. 활성 선택 영역으로 폴백해 다른 문단으로 이동하거나 치환해서는 안 됩니다.

`pendingCommands`는 적용 결과를 정확한 카드에 연결하는 단기 트랜잭션 레지스트리입니다. 완료 아카이브의 장기 위치 추적 저장소로 확대하지 말고, 완료 카드에는 별도의 적용 메타데이터를 남기며 위치 조회는 매번 최신 문서 상태를 확인하는 구조가 좋습니다.

## 4. 수정 이력 캐시와의 관계

두 기능은 보완적이지만 같은 데이터로 합치면 안 됩니다.

- 완료 아카이브: 특정 문서의 특정 카드에 대해 사용자가 실제 적용한 작업 이력이다.
- 수정 이력 캐시: 다른 문단/문서에서 유사 오류가 재발했을 때 참고할 수 있는 일반화된 교정 지식이다.

완료 기록을 이력 캐시의 후보 데이터로 사용할 수는 있습니다. 다만 자동 적용의 근거로 바로 쓰면 문맥 차이, 여러 발생 위치, 사용자 사후 수정 때문에 오수정 위험이 큽니다. 캐시는 기본적으로 "재사용 제안"까지만 하고, 새 문단의 최신 텍스트·해시로 별도 QA 카드와 명령을 만들어야 합니다. 중복 방지를 위해 캐시 키는 단순 category/original/suggested 조합보다 언어, 문맥, 가이드라인 버전, 사용자 확정 여부를 포함하는 편이 좋습니다.

## 5. UI 우선순위와 권장 범위

진행 순서는 다음이 합리적입니다.

1. Task B 완료 및 문단 조회 실패/문서 불일치 처리 검증
2. "위치 보기" 전용 명령과 사용자 피드백 구현
3. 기존 `appliedCards`를 사용하는 "완료 보기" 토글 또는 별도 탭, 완료 수 표시, 읽기 전용 상세 화면
4. "재확인"으로 최신 카드 또는 micro QA 재분석을 만드는 흐름
5. 수정 이력 캐시 설계 및 제안 재사용 기능

"완료 보기"는 활성 목록의 severity/category 검색 결과와 섞지 말고, 별도 보기 모드로 두는 편이 사용자가 현재 해결할 일과 과거 이력을 혼동하지 않습니다. 완료 상세에는 적용 시각, 적용 전후 제안, 적용 당시/현재 상태, 위치 보기와 재확인 동작을 보여 주면 충분합니다. 재확인은 즉시 치환 버튼보다 우선되어야 하며, 최신 검증 결과가 확보된 뒤에만 적용을 허용해야 합니다.

## 확인이 필요한 제품 결정

- 완료 아카이브를 앱 세션 안에서만 유지할지, 재시작 후에도 문서별로 보존할지
- 원래 문단을 찾지 못했을 때 완료 이력을 유지한 채 "고아 항목"으로 표시할지, 보존 기간을 둘지
- 재확인 시 동일 제안을 보여 주는 조건(완전한 본문/해시 일치만 허용하는지)과 불일치 시 자동 micro QA 재분석 여부
- 수정 이력 캐시의 자동 적용 허용 범위. 초기 버전은 자동 적용 없이 제안만 하는 것이 안전함

본 문서는 검토 의견이며 코드 변경이나 구현 지시는 포함하지 않습니다.
