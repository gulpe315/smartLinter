# 해결된 QA 카드가 남는 현상: 원인 진단과 해결 방향

## 결론

이번 관찰만으로는 옛 카드와 최신 텔레메트리의 `paragraphId`가 실제로 달랐다고 **확정할 수는 없다**. 화면/로그에 두 전체 ID와 그에 대응하는 QA 분석 결과가 남아 있지 않기 때문이다.

다만 현재 구현에서 이 카드가 남을 수 있는 직접 조건은 분명하다. `addReport`는 최신 **QA 보고서**의 `paragraphId`가 카드의 `paragraphId`와 완전히 같을 때만 pending 카드를 정리한다. 최신 텔레메트리 자체는 카드를 정리하지 않는다. 따라서 아래 둘 중 하나면 재현된 화면이 가능하다.

1. 최신 텔레메트리의 ID가 카드의 ID와 달라졌다. InDesign ID가 `indesign-para-{storyId}-{paragraphIndex}`이므로, 같은 Story 앞쪽의 문단 삽입/삭제/분할/병합으로 index가 바뀌면 실제로 가능한 경로다.
2. 최신 텔레메트리에 대한 QA 분석/`addReport`가 아직 완료되지 않았거나 실패했다. 분석은 `new-paragraph-detected` 뒤 1초 디바운스 후 시작되고, 성공적으로 반환된 보고서에서만 `addReport`가 호출된다.

현 상태에서는 1번이 더 유력한 구조적 취약점이지만, 2번을 배제할 관측값이 없다. 특히 “상단 텔레메트리에 `일요일`이 보인다”는 것은 분석 결과(`PASS`, `issues: []`)가 이미 도착했다는 증거가 아니다.

`위치 보기`의 NOT_FOUND 결과는 별도의 중요한 사실을 준다. Task C의 탐색은 저장된 index가 맞지 않으면 같은 Story 전체에서 카드 생성 당시의 `baseHash`를 찾는다. 그러므로 이 결과는 **카드 생성 당시 전체 문단(`일오일`을 포함한 원문)의 hash와 같은 문단이 현재 그 Story에 하나도 없거나, 안전하게 단일 후보로 정할 수 없었다**는 뜻이다. 이는 원문이 직접 수정되었다는 관찰과 일치한다. 그러나 그것만으로 ID 변경 여부나 `addReport` 호출 여부는 판별할 수 없다.

## 코드 근거

- `text_observer.jsx`는 ID를 Story ID와 `targetParagraph.index`로 구성한다. 이 값은 문단 자체의 지속 ID가 아니라 위치 ID다.
- `qaStore.ts`의 `addReport`는 `card.paragraphId !== payload.paragraphId`이면 무조건 카드를 보존한다. 같은 ID라면 `issues: []`인 최신 보고서가 해당 pending 카드를 제거한다.
- `qaStore.ts`의 텔레메트리 listener는 즉시 정리하지 않고, 1초 뒤 `analyzeParagraph`를 호출한 뒤 성공한 결과로 `addReport`를 호출한다. 오류/응답 지연/구독 미초기화 시에는 기존 카드가 남는다.
- 현재 단위 테스트는 같은 ID로 `addReport({ issues: [] })`를 직접 호출했을 때의 제거만 검증한다. “문단 위치 이동으로 ID가 바뀐 뒤, 새 텍스트가 clean인 경우”의 end-to-end 회귀 테스트는 없다.

## `baseHash` 복구를 `addReport`에 그대로 적용할 수 없는 이유

Task C의 hash 기반 탐색은 **문단 내용이 아직 변경되지 않았고 위치만 이동한 경우**에는 적합하다. 반면 이번 카드는 사용자가 `일오일`을 `일요일`로 직접 고친 상황이다. 그 순간 문단 hash가 바뀌므로, 옛 `baseHash`로는 같은 문단을 재발견할 수 없다. 즉 Task C의 알고리즘을 프런트엔드에 복사해도 이번 “이미 고쳐진” 사례는 해결하지 못한다.

또한 “같은 문서에서 `originalSegment`를 포함한 문단이 더 이상 없으면 제거”를 **활성 문단 텔레메트리 한 건**으로 판단하면 위험하다. 텔레메트리는 현재 선택/편집 중인 문단만 보낸다. 다른 문단의 카드까지 잘못 지울 수 있고, 같은 원문 조각이 여러 문단에 반복될 수도 있다. 이 기준은 전체 문서의 권위 있는 스캔 결과가 있을 때에만 보조 신호가 될 수 있으며, 단독 식별자는 될 수 없다.

## 권장 해결 방향

### 1. 단기: 원인을 관측 가능하게 하고, 같은 ID 경로를 먼저 확정

다음 재현에서 아래 값을 같은 correlation ID 또는 timestamp로 남겨야 한다. 본문 전체 대신 ID, hash 앞 12자리, 길이만 기록하면 된다.

- 카드 생성 시: `cardId`, `paragraphId`, `paragraphHash`, `originalSegment` 길이
- 최신 telemetry 수신 시: `paragraphId`, `hash`, text length
- 분석 시작/성공/실패 시: telemetry의 `paragraphId`, request version, QA issue 수, 오류
- `addReport` 전후: payload ID와 제거한 카드 ID/ID 목록
- 위치 보기 시: 요청 ID/baseHash 앞자리, `findParagraphById`의 fast-path/scan-path 결과와 후보 수

이 관측으로 (a) 새 ID여서 정리 조건을 못 맞춘 경우, (b) 분석 결과가 도착하지 않은 경우, (c) 보고서가 여전히 issue를 반환한 경우를 한 번의 재현으로 구별할 수 있다. 현 보고서의 사실만으로 1번을 단정해 코드 변경을 시작하는 것은 피하는 편이 안전하다.

### 2. 단기 안전책: 위치 ID가 오래된 카드를 ‘현재성 미확인’으로 취급

문단을 직접 수정한 뒤 동일 ID의 clean QA 보고서가 오면 기존처럼 제거한다. 반대로 위치 이동/재식별 실패가 확인된 카드는 계속 활성 `pending`으로 두지 말고, 적용과 위치 보기의 위험을 명확히 표시하는 별도 상태(예: `stale_unresolved`)로 전환하는 방향을 권장한다. 이 상태에서는 자동 적용을 막고, 최신 문단을 다시 분석하거나 사용자가 카드를 해제하도록 안내한다.

이는 확실하지 않은 카드의 자동 삭제보다 안전하다. 특히 현재 `위치 보기`가 NOT_FOUND를 반환한 카드는 이미 대상 문단을 안전하게 식별할 수 없다는 신호이므로, 활성 적용 버튼을 유지할 이유가 없다.

### 3. 근본 해결: 지속 가능한 문단 앵커를 설계

카드 수명과 문단 이동을 제대로 연결하려면 `storyId + paragraphIndex` 대신, 문서 세션을 넘어서도 유지되는 식별/재식별 정보가 필요하다.

- 가능하면 InDesign 문단 또는 이에 준하는 범위에 지속 가능한 label/UID를 부여하고, 텔레메트리·QA 카드·치환·위치 보기에 동일 값을 전달한다.
- 문서 자체를 라벨링할 수 없다면, `document identity + storyId + 문단 전후 문맥 fingerprint + 생성 시 위치`를 앵커로 저장하고, 텔레메트리/재스캔 시 단일 후보만 재결합한다.
- 후보가 0개 또는 여러 개면 자동으로 연결·삭제·치환하지 않는다. 사용자에게 재스캔 또는 재선택을 요구한다.

이 앵커가 있어야 “변경 전 카드의 문단”과 “변경 후 clean telemetry의 문단”을 같은 개체로 판단할 수 있다. 해시만으로는 내용 변경 후의 동일성을 증명할 수 없다.

### 4. 전체 문서 스캔을 도입할 때의 정리 규칙

향후 권위 있는 전체 문서 스캔이 제공된다면, 그 스캔의 문단 앵커 집합과 QA 결과를 기준으로 카드 상태를 reconcile할 수 있다. 이때도 단순 문자열 포함 여부가 아니라 앵커별 최신 QA 결과를 우선해야 한다.

- 스캔에서 같은 앵커의 `PASS`/빈 issues가 확인되면 해당 pending 카드를 제거한다.
- 앵커가 사라졌거나 모호하면 카드를 자동 삭제하지 않고 `stale_unresolved` 또는 별도 보관함으로 이동한다.
- 스캔 범위 밖 문단의 카드는 보존한다. 부분 스캔 결과로 문서 전체의 카드를 정리해서는 안 된다.

## 필요한 회귀 테스트

수정 단계에서는 최소한 다음을 추가해야 한다.

1. 같은 ID의 clean 재분석은 현재처럼 해당 pending 카드만 제거한다.
2. ID가 바뀐 clean telemetry/보고서는 옛 카드를 잘못 삭제하지 않으며, 설계된 stale 상태로만 전환한다.
3. 앞 문단 삽입으로 index가 바뀐 뒤에도 지속 앵커가 있으면 같은 카드가 최신 문단에 연결되어 clean 결과에서 제거된다.
4. 원래 문단이 삭제됨, 동일 문맥/해시 후보가 둘 이상임, 다른 문단에 같은 `originalSegment`가 있음의 경우 다른 카드를 삭제하거나 치환하지 않는다.
5. QA 분석 실패·timeout·listener 미초기화 상황에서는 telemetry만 보고 해결된 것으로 처리하지 않는다.

## 판단

현재 Task A의 “같은 `paragraphId`에 대한 최신 보고서가 권위 있다”는 규칙 자체는 옳지만, InDesign의 위치 기반 ID를 안정 ID로 전제한 범위에서만 성립한다. 이번 버그의 즉시 원인은 그 전제가 깨졌거나 최신 분석 결과가 아직 없다는 데 있으며, 현 자료로 둘 중 하나를 확정할 수는 없다. 장기 해법은 `originalSegment` 전역 검색이 아니라 문단 지속 앵커와 스캔 범위가 명확한 reconciliation이다.

코드 수정은 수행하지 않았다.
