# 재조율 요청 — 번역 모드 T0 설계 자문 상충 3건

Codex와 agy 양쪽에 `DESIGN_REQUEST_TRANSLATION_MODE_T0.md`로 같은 설계
자문을 요청했다(`CODEX_ANSWER_TRANSLATION_MODE_T0.md`,
`AGY_ANSWER_TRANSLATION_MODE_T0.md`). 질문 1(세그먼트 단위=문장)과
질문 6(Word/InDesign 둘 다 지원)은 완전히 수렴했다. 질문 4(UI 진입점)는
"T1은 사용자용 전용 화면 없음"이라는 결론 자체는 같지만 Header 토글
배지 하나를 넣을지 여부에서 미세하게 갈렸다. 아래 3개 쟁점은 명시적으로
갈렸다 — 어느 한쪽 편을 들지 않고 상충 자체와 근거를 그대로 보여줄
테니, 상대 주장을 검토한 뒤 **각 쟁점마다 최종 권장안 하나**를 근거와
함께 달라(둘 중 하나를 고르거나, 조건부 절충안을 제시해도 됨).

## 쟁점 1: 세션 진입/멤버십 모델 — 자동 편입 vs 명시적 추가

**agy 안 — 번역 모드 ON 상태의 telemetry 기반 자동 Upsert:**
> Codex 로드맵의 "선택 문단의 source/target을 세션에 누적"은 에디터에서
> 포커스/선택되어 텔레메트리로 수신된 `activeParagraph`를 세션
> 저장소에 자동 Upsert하는 구조를 의미한다... 번역 모드가 켜져 있는
> 동안 이 텔레메트리를 수신하여 세션 스토어에 자동으로 문장 세그먼트를
> 누적하는 방식은 매 문단마다 "세션에 추가" 버튼을 누르게 하는
> 불필요한 마찰을 완전히 제거한다.

**Codex 안 — 사용자가 명시적으로 지정한 문단만 추가:**
> T1 정의의 "선택 문단"은 방문 telemetry 전체가 아니라 의도적으로
> 지정한 범위를 뜻한다... 방문 문단 누적은 빠른 프로토타입에는
> 가능하지만 완전성을 보장하지 못하며, 아직 방문하지 않은 문단이
> 빠진다는 한계도 명시돼 있다(`CODEX_ANSWER_AUTO_TRANSLATE_AND_TRANSLATION_MODE.md:226,228`).
> 따라서 T1의 입력 API는 `addParagraphToTranslationSession(paragraph)`
> ... 자동 수집은 T3의 전체/범위 스캔과 혼동되므로 T1에 넣지 않는다.

**참고**: Codex가 인용한 226/228번 줄의 "불완전성" 경고는 원래 로드맵
문서에서 "정식 기능이라면"(즉 T3 전체 스캔 단계) 완전성이 필요하다는
맥락으로 보이는데, T1은 애초에 "문장 ID·순서·stale 모델 확인"이 목적인
스파이크이지 완전한 XLIFF를 만드는 단계가 아니다. 이 경고를 T1에도
그대로 적용해야 하는지, 아니면 T3에만 해당하는 경고인지도 답변에서
짚어달라.

## 쟁점 2: target 초기값 — TM 자동 채움 vs 항상 빈 값

**agy 안 — TM 100% Exact-유일 매치만 자동 채움:**
> Target 초기화: TM 100% Exact-유일 매치(`eligible`)만 target 초안으로
> 자동 채우고, 충돌(`conflict`), Fuzzy(75~99%), 매치 없음은 빈 상태로
> 시작한다... `tmAutoApplyObservation.ts`의 `deriveTmAutoApplyPlan`을
> 그대로 실행하여, `eligible`인 경우 `candidate.target`을 초기 target
> 값 및 `origin: 'tm-exact'` 메타데이터로 세팅한다.

**Codex 안 — target은 항상 빈 값, TM 후보는 메타데이터로만 기록:**
> T1은 target 필드를 갖되 기본값은 빈 값이다. TM 후보는 출처
> 메타데이터로만 기록하며, 자동 채움·target 편집 UI·AI 번역 연동은
> T1 범위 밖이다... T1은 세그먼트별로 후보 목록과 score/origin은
> 수집하되, `targetDraft: ''`로 시작한다. `TmAutoApplyPlan`을
> 재사용해 target을 자동 확정하지 않는다.

두 안 모두 "T1은 편집 UI/AI 연동을 포함하지 않는다"는 점은 동의한다 —
차이는 오직 "세그먼트가 세션에 들어오는 그 순간, TM 100% 유일 매치가
있으면 target 필드에 미리 채워 넣어두는가"이다. 이게 정말 T1(데이터
모델 스파이크)의 스코프에 필요한지, 아니면 "채워는 두되 사용자가 손댄
적 없다는 표시(origin)만 남기는" 정도로 충분한지 판단해달라.

## 쟁점 3(가장 중요): 세션 수명 — 인메모리 전용 vs 영속 필수

**agy 안 — T1도 인메모리 전용, 영속화 불필요·금지:**
> T1은 "인메모리(In-Memory) 전용, 앱 재시작 시 소멸" 정책을 채택한다.
> 브라우저 저장소나 로컬 파일로의 비동기 자동 영속은 불필요하며
> 금지한다... 앱 재시작 후 사용자가 Word/InDesign 문서를 외부에서
> 수정하거나 다른 문서를 열었을 때 로컬 저장소에 남아 있던 과거
> 세그먼트 데이터는 무효(Stale) 상태가 되어 에디터 상태와 심각한
> 충돌을 일으킨다... 이 작업 산출물의 영속화는 표준 `.xliff` 파일(T2
> 산출물)이 온전히 담당해야 한다.

**Codex 안 — T1부터 영속 필요, 단 복구 시 전부 stale로 취급:**
> 영속성이 필요하다. T1부터 앱 재시작 뒤에도 세션을 복구해야 한다.
> 번역 세션은 source/target, 식별자, 해시, 상태, TM 출처를 보관하는
> 작업 산출물이다... 이는 단기 되돌리기 로그와 성격이 다르므로 인메모리
> 전용 정책을 재사용하면 안 된다. 저장 위치·파일 포맷은 이번 T0에서
> 고정하지 않는다. **단, 복구 직후에는 마지막 캡처 해시를 신뢰해 자동
> export/자동 적용하지 않고, 세그먼트를 `stale/needs-validation`으로
> 복원하는 정책을 T1의 수용 조건으로 둔다.**

**짚어줄 점**: agy가 반대 근거로 든 "재시작 후 외부 문서 변경으로 인한
stale 충돌"은, Codex의 안이 이미 "복구된 모든 세그먼트를 무조건
stale/needs-validation으로 표시"하는 것으로 정확히 방어하고 있는 것
같다 — 즉 Codex 안대로면 agy가 우려하는 시나리오(오래된 데이터를
신뢰해서 사고가 나는 것)는 애초에 발생하지 않고, 대신 "사용자가 오래
공들인 세션이 완전히 사라지는 것"만 막는다. agy가 이 방어책을 반영해도
여전히 인메모리를 권장하는지, 아니면 이 방어책이면 영속으로 의견을
바꾸는지 명확히 답해달라(트랙 B Stage A에서 agy가 자기 코드로 재확인한
뒤 원안을 철회한 전례처럼, 상대 주장을 실제로 검토한 뒤 결론 내려달라).

## 요청

1. 위 3개 쟁점 각각에 대해 최종 권장안 하나를 정하고, 상대 주장의 어느
   부분이 틀렸는지/불충분한지 구체적으로 반박하거나, 왜 절충 가능한지
   설명할 것.
2. 쟁점 3은 특히 "짚어줄 점"에서 제시한 반박을 반드시 검토하고 응답할
   것 — 단순히 원안을 반복하지 말 것.
3. 결론이 이전 답변에서 바뀐다면 무엇이 결정적이었는지 명시할 것.
4. 답변은 파일로 저장하지 말고 응답 텍스트로 전체를 직접 출력할 것(각
   프로세스가 파일 쓰기 권한이 없을 수 있음 — Claude가 받아서 저장한다).
