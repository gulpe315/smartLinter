# 재조율 요청 — 번역 모드 T3 설계 자문 상충 1건 + 확인 1건

Codex와 agy 양쪽에 `DESIGN_REQUEST_TRANSLATION_MODE_T3.md`로 같은 설계
자문을 요청했다(`CODEX_ANSWER_TRANSLATION_MODE_T3.md`,
`AGY_ANSWER_TRANSLATION_MODE_T3.md`). 질문 1(paragraphId 스킴 — 둘 다
`word-para-body-<index>-<hash>` 형태의 위치+해시 합성 ID로 수렴, 정확히
같은 포맷을 독립적으로 제시함), 질문 2(Word는 기존
`queryLiveParagraphSnapshots`와 별개의 신규 전체 열거 함수로 분리),
질문 4(비파괴적 병합, `targetDraft` 절대 보존), 질문 5(export 시점
1회 온디맨드 재검증, 상시 폴링 금지), 질문 7(Word 선행 → InDesign 후속
2단계 착수)은 실질적으로 완전히 수렴했다. 아래 1개는 명시적으로 갈렸고,
1개는 사소하지만 확인이 필요하다.

## 쟁점 1(질문 3 관련): InDesign에서 미배치/오버셋 스토리를 포함할지 제외할지

**Codex 안 — 연결되지 않은(unplaced) Story도 포함해야 한다:**
> 연결되지 않은 Story도 문서에 실제 존재하는 번역 대상일 수 있으므로,
> "텍스트 프레임에 연결된 본문만"으로 제한하면 조용한 누락 위험이 너무
> 크다. ... 포함: `Document.stories`의 지원되는 일반 paragraph 범위,
> 복수 Story 포함 / 제외: 표 셀 텍스트, 각주/미주, 주석, 지원하지 않는
> 특수/중첩 텍스트 범위. **연결되지 않은 Story는 제외하지 않는다.**

**agy 안 — 미배치/오버셋 스토리는 제외해야 한다:**
> [InDesign T3 수집 범위] 제외 대상: Table Cells(표 내부 문단), Footnotes
> / Endnotes(각주/미주 문단), **Overset / Unplaced Stories(프레임에
> 배치되지 않은 숨김 텍스트)**.

두 안이 정확히 반대 결론이다. 판단에 참고할 점을 짚어달라:

1. "Unplaced story"(어떤 텍스트 프레임에도 스레드되지 않고 `doc.stories`
   컬렉션에만 존재하는 스토리)와 "overset text"(프레임에는 배치돼
   있지만 프레임 크기보다 텍스트가 많아 화면에 안 보이는 부분)는
   InDesign에서 서로 다른 개념이다 — agy의 답변은 이 둘을 함께 묶어
   "숨김 텍스트"로 제외했는데, Codex의 답변은 "연결되지 않은 스토리"만
   언급하고 overset은 따로 짚지 않았다. 이 개념 차이를 인지한 뒤에도
   각자 결론이 같은지 확인해달라 — 특히 agy는 "unplaced"와 "overset"을
   따로 판단해도 결론이 같은지(둘 다 제외) 명확히 해달라.
2. **실사용 시나리오 근거**: 실제 InDesign 문서에서 unplaced story가
   생기는 대표적 원인은 (a) 사용자가 텍스트 프레임을 삭제했지만 연결된
   스토리 데이터가 남아있는 경우(사실상 죽은 데이터), (b) 스크립트/
   플러그인이 임시로 만들어 둔 미배치 텍스트, (c) 실제로 나중에 배치할
   예정인 초안 텍스트. Codex는 (c) 같은 경우를 "조용한 누락"으로
   우려하는 것으로 보이는데, agy는 애초에 이런 텍스트가 "번역 대상
   문서"로서의 실질을 갖는지에 회의적인 것으로 보인다. 어느 쪽이
   실무적으로 더 흔하고 더 위험한 실패 모드인지 각자 근거를 대달라.
3. **절충안 가능성**: 완전히 포함하거나 완전히 제외하는 것 외에,
   "unplaced/overset도 수집은 하되 `partial-coverage`/`excluded` 항목
   개수와 별도로 명시적으로 표시해 사용자가 판단하게 하는" 중간 지점이
   있을 수 있다(agy 자신도 §3에서 표/각주 제외 시 "제외 항목 통지 UX"를
   이미 제안했다 — 같은 통지 메커니즘을 unplaced/overset에도 적용하면
   "제외하되 숨기지 않는" 절충이 되는지 판단해달라).

## 확인 1(질문 6 관련, 재조율까지는 아니고 가벼운 정렬): 취소(cancel) 처리 깊이

두 답변 모두 "T3 스캔은 로컬 전용이라 매우 빠르다(agy: 500문단 기준
0.5초 이내)"는 데는 동의하지만, 그로부터 이끌어낸 취소 설계의 깊이가
다르다.

- Codex: "host 플러그인의 chunk 경계에서 협력적으로 취소하고, 기본적으로
  부분 inventory를 세션에 병합하지 않음"(청크 단위 협조적 취소 메커니즘을
  실제로 구현).
- agy: "순수 로컬 수집은 거의 즉시 끝나므로 복잡한 비동기 작업 취소
  핸들러보다는 10초 타임아웃 가드와 스캔 실패 시의 상태 복구를 중심으로
  구현"(정교한 취소 메커니즘 자체를 사실상 불필요하다고 봄).

이건 상충이라기보다 "얼마나 정교하게 만들 것인가"의 정도 차이로 보인다
— 어느 쪽이든 "실패/취소 시 부분 결과를 세션에 절대 병합하지 않는다"는
결과는 같다. 굳이 재반박할 필요는 없고, 각자 왜 그 정도의 메커니즘이면
충분하다고(또는 부족하다고) 보는지 한 줄씩만 확인해달라 — 실제 구현
지시서에서 과설계 없이 정할 수 있게.

## 요청

1. 쟁점 1은 반드시 하나의 최종 결론(포함/제외/절충안 중 하나)으로
   수렴해서 답할 것 — 상대 주장의 어느 부분이 틀렸는지/불충분한지
   구체적으로 반박하거나, 왜 절충이 맞는지 설명할 것.
2. 확인 1은 위에서 설명했듯 가볍게만 답해도 된다.
3. 결론이 이전 답변에서 바뀐다면 무엇이 결정적이었는지 명시할 것.
4. 답변은 `{CODEX|AGY}_RECONCILED_TRANSLATION_MODE_T3.md` 내용으로,
   파일로 저장하지 말고 응답 텍스트로 전체를 직접 출력할 것(Claude가
   받아서 저장한다).
