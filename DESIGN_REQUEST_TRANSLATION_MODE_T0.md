# 설계 자문 요청 — 트랙 C: [번역 모드]+XLIFF T0(요구사항 고정)

## 배경

트랙 A(QA 카드 Mode A)와 트랙 B(TM 자동 치환 A/B/C)가 전부 완료됐다.
사용자가 다음으로 트랙 C(번역 모드+XLIFF) 착수를 승인했다. Codex 로드맵
(`CODEX_ANSWER_AUTO_TRANSLATE_AND_TRANSLATION_MODE.md` "번역 모드 단계별
권고" 표, 줄 329~340)은 T0(요구사항 고정)를 "범위 불명확하면 구현 착수
금지"인 게이트로 정의한다. 이 설계 자문의 목적은 T1(번역 세션 스파이크)
착수 전 구체적인 스코프를 고정하는 것이다 — 전략적 방향(사이드카 vs
새 문서 생성 vs bilingual 편집)은 이미 두 자문이 합의했으므로 재론하지
않는다.

## 이미 합의된 것(재확인만, 재검토 요청 아님)

- **사이드카 모델 채택**: XLIFF는 내부 canonical 계약이 아니라 번역
  세션에서 파생되는 사후 산출물이다(`CODEX_ANSWER_...:190-210`,
  `AGY_ANSWER_...:51-58`). Phase 0의 "에디터에 열린 문서가 원문, QA는
  monolingual" 결정과 공존한다.
- **에디터 원본에 직접 이중언어 병기 후 클린업(해석 c)은 절대 금지**
  (`CODEX_ANSWER_...:275-285`, `AGY_ANSWER_...:208` — 과거 폐기된 "인접
  삽입" 기능보다 훨씬 위험한 대량 편집으로 두 자문 모두 명시적 반대).
- **새 번역 문서 생성(해석 b)은 장기 과제로 보류**, 이번 트랙 범위 밖.
- **첫 XLIFF 타깃은 plain-text XLIFF 1.2**, 인라인 태그 보존은 T4에서
  별도 다룬다(`CODEX_ANSWER_...:287-327`). 현재 TMX 파서가
  `<bpt>`/`<ept>`/`<ph>` 등을 제거하고 있어(`tmx_parser.rs:163`) 태그
  보존은 T1 범위가 아니다 — plain text만으로 시작.
- **T1은 "문서 전체 스캔"이 아니다.** 현재 인프라는 사용자가 방문/편집한
  문단만 telemetry로 받는 구조이고, 전체 문서 스캔은 `start_batch_scan`
  류의 별도 인프라(T3)가 선행돼야 한다(`CODEX_ANSWER_...:226-234`,
  `RECONCILED_TM_AUTO_APPLY_STAGE_B.md`에서도 같은 이유로 "현재 문단
  한정"으로 범위를 좁힌 전례가 있다). T1은 "사용자가 번역 모드로 방문한
  문단만 세션에 누적"으로 시작해야 한다는 게 이번 자문의 전제다 — 이
  전제 자체에 이견이 있다면 지적해달라.

## 요청하는 것 — T1 착수를 막고 있는 구체적 미결정 사항

1. **번역 세션의 세그먼트 단위.** TM/QA는 이미 문장 단위 경계 계약이
   있다(`splitIntoSentences`, `TmSentenceMatch`,
   `tmAutoApplyObservation.ts`의 `segmentIndex`). 번역 세션도 문단이
   아니라 **문장 단위**를 번역 단위(trans-unit 후보)로 삼아야 하는지
   확인해달라 — XLIFF의 `<trans-unit>`이 보통 문장 또는 그보다 작은
   세그먼트 단위인 점, 그리고 기존 TM 인프라를 그대로 재사용할 수 있는
   실익을 감안하면 문장 단위가 자연스러워 보이는데 동의하는지, 아니면
   T1은 문단 단위로 단순화하고 문장 분할은 T2(export) 시점으로 미뤄야
   하는지 판단해달라.
2. **세션 진입/멤버십 모델.** "번역 모드"가 켜져 있는 동안 사용자가
   방문하는 모든 문단이 자동으로 세션에 편입되는지(QA/TM처럼 상시
   telemetry 기반 자동 편입), 아니면 사용자가 문단/문장을 명시적으로
   "번역 세션에 추가"해야 하는지 정해달라. 전자는 기존 telemetry 파이프
   라인을 그대로 재사용할 수 있어 구현이 단순하지만, 사용자가 의도치
   않은 문단까지 세션에 쌓일 수 있다. 후자는 명시적이지만 신규
   상호작용을 요구한다. Codex 로드맵의 "선택 문단의 source/target을
   세션에 누적"이라는 표현(`CODEX_ANSWER_...:334`)이 어느 쪽을
   가리키는지 포함해서 답해달라.
3. **target의 초기 출처.** 문단/문장이 세션에 들어올 때 target 초안이
   자동으로 채워지는지 — 예를 들어 기존 TM exact/fuzzy 후보가 있으면
   자동으로 채워 넣고(트랙 B Stage A의 `TmAutoApplyPlan` 관찰 결과를
   재사용할 수 있는지도 판단), 없으면 빈 상태로 시작해 사용자가 수동
   입력하거나 AI 커맨드(`AICommandBar`/`execute_ai_command`)로 초안을
   받는 구조인지. 이번 T1은 "세션 누적" 스파이크이므로 target 편집
   UI/AI 연동까지 포함해야 하는지, 아니면 T1은 source만 모으고 target
   입력 UI는 다음 단계로 미뤄도 되는지 결정해달라.
4. **UI 진입점과 화면 구조.** 현재 대시보드는
   `MainLayout.tsx`(줄 55~110)가 고정된 QA(좌)/TM(우) 2분할이고,
   `Header.tsx`는 레이아웃 프리셋 전환 버튼만 있다. "번역 모드"가:
   - (a) `Header`의 새 토글 버튼으로 전환하는 별도 전체 화면 뷰(QA/TM
     패널을 대체)인지,
   - (b) 기존 2분할에 추가되는 세 번째 패널/탭인지,
   - (c) 아니면 이번 T1(순수 세션 누적 스파이크) 단계에서는 사용자에게
     보이는 전용 UI가 전혀 없이 스토어/데이터 모델만 만들고 화면은 다음
     단계(T2 export 버튼 정도)에서 최소한으로 붙이는지
   무엇이 T1 스코프로 맞는지 정해달라. agy 원안(줄 264, 280)은 "2열
   그리드 UI"를 트랙 2 자체 범위로 이미 포함시켰던데, Codex 로드맵의 T1
   정의(문장 ID·순서·stale 모델 확인, 원본 문서 변경 없음)는 UI를
   명시하지 않는다 — 이 차이를 짚어서 T1이 데이터 모델 스파이크인지 UI
   포함 스파이크인지 명확히 해달라.
5. **세션 수명과 영속성.** 트랙 B Stage C의 되돌리기 로그는 "인메모리,
   앱 재시작 시 소멸"로 확정했었다(`RECONCILED_TM_AUTO_APPLY_STAGE_C.md`
   §1) — 그 되돌리기 로그와 달리, 번역 세션은 사용자가 상당한 시간을
   들여 만든 실질적 작업 산출물이 될 수 있다. T1에서도 같은 "인메모리
   전용, 재시작 시 소멸" 정책을 따라도 되는지, 아니면 (export 이전
   단계인데도) 로컬 파일/브라우저 저장소에 최소한의 영속을 둬야 하는지
   판단해달라. 영속이 필요하다면 그 저장 방식(파일 경로, 포맷)까지 T0
   에서 정할 필요는 없고 "필요하다/불필요하다"만 정해줘도 된다 — 필요
   판정이면 다음 자문에서 구체 설계를 다룬다.
6. **호스트 범위.** T1이 Word/InDesign 둘 다 대상인지, 하나로 시작하는
   지. 기존 TM/QA 파이프라인은 이미 양쪽을 브릿지 추상화로 지원하므로
   T1(순수 세션 누적, 문서 미변경)은 호스트 특정 코드가 필요 없어 자동
   으로 둘 다 커버될 것으로 보이는데, 맞는지 확인해달라.

## 요청하지 않는 것 (범위 밖)

- T2(XLIFF export) 이후 단계의 구체 설계 — 이번은 T1 착수를 막는
  요구사항만 고정한다.
- 인라인 태그 보존(T4), 문서 전체 스캔(T3), XLIFF import/merge(T5), 새
  문서 생성(T6), bilingual 편집(T7) — 전부 이번 범위 밖.
- 사이드카 vs 다른 두 해석 중 무엇을 채택할지 — 이미 결정됨(위 참고).

## 답변 형식

`{CODEX|AGY}_ANSWER_TRANSLATION_MODE_T0.md`로, 위 1~6 각각에 명확한
결론을 근거와 함께 담아 응답 텍스트로 직접 출력해달라(파일 저장 지시
없음 — Claude가 받아 저장한다).
