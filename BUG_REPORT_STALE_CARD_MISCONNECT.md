# 버그 리포트: Stale 재스캔이 엉뚱한 카드에 연결됨

## 재현 상황 (2026-08-25, 실제 InDesign 라이브 테스트 중 사용자가 스크린샷으로 보고)

InDesign 문서에 서로 다른 위치의 문단 여러 개에 대해 QA 카드가 각각 떠 있는 상태였음
(예: 한 카드는 요일 오타 -260-4 관련 - 원문 `일오일` -> `일요일`).

사용자가 어떤 카드의 [적용] 버튼을 눌렀는데(다른 위치의 문단), 정작 그 카드가 아니라
**전혀 다른, 관련 없는 카드** 쪽에 "문서가 방금 수정되었습니다. 최신 상태로 새로고침합니다"
배지(오렌지색 stale 배너)가 뜨고 자동 재스캔(`새로고침 중...`)이 시작됨.

즉: [적용]을 누른 카드의 결과가 자기 자신이 아니라 **엉뚱한 다른 카드**에 stale/재스캔 처리로
연결되어 버려서, 원래 그 카드가 진단하고 있던 문단 위치 추적을 잃어버리는 것으로 보임.

## 요청

관련 컴포넌트: `src/services/stale_conflict_resolver.ts`(특히 `initEventListener`에서
`replacement-result` 이벤트를 받아 `STALE_REJECTED` 상태일 때 어느 QA 카드에 연결할지 결정하는
로직), `src/stores/qaStore.ts`의 `acceptCard`, InDesign 쪽 `atomic_replacer.jsx`의 결과 dispatch
경로까지 관련 흐름 전반을 검토해서:

1. 왜 [적용]을 누른 카드가 아니라 다른 카드가 선택되는지 원인을 진단해줄 것.
2. 근본 원인에 맞는 수정 방향을 제안해줄 것(단, 아직 코드 수정은 하지 말고 분석/제안만).

코드를 직접 고치기 전에, 진단 결과를 Claude가 다른 모델(agy/Codex 상호 교차) 의견과 종합해서
최종 결정할 예정이니 이 요청에서는 원인 분석과 해결 방향 제안까지만 부탁함.
