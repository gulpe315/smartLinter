# 문장 경계 = SRX(Trados류 번역단위) 채택 시 기존 "문장카드 반대" 결론 재평가

## 배경
직전 자문(`AGY_ANSWER_LOCATE_WORD_AND_SENTENCE_SCOPING.md`,
`CODEX_ANSWER_LOCATE_WORD_AND_SENTENCE_SCOPING.md`)에서 두 분 다
"마침표 등 임의 규칙으로 문장을 나누면 한국어에서 오분리 위험이 크다
(Dr./e.g./인용부호/줄임표 등)"는 이유로 문장 단위 카드/분석을 반대하고,
대신 "데이터 모델은 그대로 두고 UI에서만 문장 그룹핑" 절충안을
제안했습니다.

사용자가 그 뒤 핵심 정보를 추가로 제시함: **"문장 구분 방법은 Trados 같은
CAT 툴의 번역단위(Translation Unit) 분할 방식과 일치시키면 된다. 그래야
나중에 TM과도 일치한다."** 즉 임의 규칙이 아니라 **SRX(Segmentation
Rules eXchange, ISO/LISA 표준 — Trados/memoQ/Wordbee/Phrase 등 CAT 툴
업계 표준)**를 채택하자는 제안입니다.

## Claude가 가볍게 확인한 사실 (검증 요청 전 최소 확인만)
- Rust 크레이트 `srx`(https://docs.rs/srx, https://github.com/bminixhofer/srx)가
  존재함 — SRX 2.0 표준의 "대체로 준수(mostly compliant)" 구현,
  `regex` 크레이트 기반. **완전 표준 준수는 아니라고 스스로 명시.**
- SRX는 실제로 Trados/memoQ/Wordbee/Phrase 등 CAT 업계에서 폭넓게 쓰이는
  성숙한 표준이 맞음(다수 벤더 문서로 교차확인).
- **Claude가 확인 못한 것(모델 자문 필요):** 실제 배포 가능한 한국어
  SRX 규칙 파일(`ko.srx`류)이 오픈소스로 존재하는지, 있다면 라이선스는
  어떤지, 품질(직함/약어/줄임표/인용부호 등 한국어 특유 함정을 얼마나
  잘 처리하는지)이 실사용에 충분한지는 검색만으로 확신 못함.

## 재평가 요청
1. **SRX 채택이 직전 자문의 핵심 반대 사유(한국어 문장경계 오분리
   리스크)를 실제로 해소하는가?** 표준 규칙 엔진을 쓴다고 해도 한국어용
   기본 규칙셋 자체의 품질이 낮으면 여전히 위험할 수 있음 — 실제 공개된
   한국어 SRX 규칙의 존재/품질을 조사(가능하면 웹검색)해서 판단해줘.
   없거나 부실하면 "허용 가능한 최소 규칙셋을 직접 만들어야 하는가"도
   같이 검토.
2. **직전 자문이 지적한 다른 반대 사유(LLM 컨텍스트 유실, 호출 비용
   3~5배 증가)는 SRX와 무관하게 여전히 유효한가?** 즉 SRX는 "경계를
   어떻게 정확히 찾을지"의 문제만 풀 뿐, "문단 대신 문장 단위로 LLM을
   호출하면 문맥을 잃는다"는 문제와는 별개라고 보이는데, 맞는지 확인.
   (Claude 의견: LLM 호출 자체는 여전히 문단 전체 단위로 하고, SRX
   경계는 **UI 표시/카드 그룹핑 및 향후 TM 정렬 목적으로만** 쓰는 절충이
   가능해 보이는데 — 이게 두 분이 이미 제안한 "Visual Sentence
   Grouping" 절충안의 그룹 경계 계산 방법을 "임의 규칙"에서
   "SRX 표준"으로 업그레이드하는 것과 동일한 방향인지 확인 부탁.)
3. **TM 정합성 측면에서 실제로 이득이 있는가?** 이 프로젝트의 기존
   TM 인프라(`TmEntry`, fuzzy match, `tmReference`)가 이미 어떤 단위로
   TM을 저장/매칭하는지 코드로 확인하고, SRX 기반 세그먼트 경계가 실제로
   TM 엔트리 경계와 더 잘 맞아떨어질지, 아니면 이 프로젝트의 TM이 이미
   문장 단위가 아니라 다른 단위(예: 전체 문단, 또는 이미 세그먼트화된
   TMX 파일 그대로)라 이득이 제한적인지 확인.
4. **채택한다면 구현 위치와 비용.** `srx` Rust 크레이트를 Rust 백엔드에
   붙이는 게 나은지, 아니면 JS/TS 쪽(프론트/Word플러그인)에 동등한
   라이브러리가 있는지, 어느 쪽이 이 프로젝트 구조상 더 적합한지.
   기존 반대 결론(전면 재설계 금지, UI만 그룹핑)을 유지한 채로 SRX를
   "그룹 경계 계산 함수"로만 좁게 도입하는 최소 스코프를 제안해줘.

## 결론 형식
직전 자문의 "문장카드 전면반대, UI그룹핑만 허용" 결론 자체를 뒤집을
근거가 있는지, 아니면 "그룹 경계를 SRX로 계산"이라는 좁은 범위로만
결론이 보강되는지 명확히 정리해줘. 코드 수정은 하지 말고 분석/의견만.
