# Word 연결 성공 후 "로컬모델 동작 안 함" 원인 자문 요청

## 상황
Word 사이드로딩(브릿지 연결)은 성공한 상태에서 실제 사용 중 사용자가
"로컬모델 동작 안 해"라고 보고함. 구체적으로 어떤 화면/동작에서 안 되는지는
아직 사용자에게 추가로 확인 못 함(원인불명 즉시자문 원칙에 따라 코드부터
깊이 파지 않고 먼저 이 상태로 자문 요청).

## 확인된 사실 (가벼운 체크만 수행, 코드 추적 안 함)
- `curl http://127.0.0.1:49152/health` → `{"connected":true,"activeEditor":"Word","sessionId":"..."}`
  (브릿지 연결 자체는 정상)
- `curl http://127.0.0.1:11434/api/tags` → 정상 응답, 모델 다수 로드됨
  (exaone3.5:7.8b 포함 — 과거 세션 기록상 이 프로젝트가 실제 사용 중인
  모델 중 하나)
- Rust 백엔드는 이번 세션에 안 건드림(오늘 변경한 건 `bridge_client.ts`
  WS→REST 폴백 수정뿐, Word 플러그인 TS 레이어only)

## 알려진 과거 유사 사고 (참고용, 이번에도 같은 원인인지는 미확인)
1. (2026-08-25) `analyze_paragraph`/`execute_ai_command` Rust 커맨드가
   원래 미등록이라 QA 분석이 항상 Mock 폴백되던 구조적 공백 (Task 15.5,
   InDesign에서 발견·수정된 사례 — Word에서도 동일 커맨드를 쓰는지 확인
   필요).
2. (2026-08-25) Task 19 시나리오 1: `MicroScopingQueue`가 앱 재시작마다
   하드코딩된 기본 모델로 리셋되는데, 사용자가 Settings에서 골라둔 모델이
   재동기화 안 되는 문제(`syncSelectedModel()` 수정으로 해결됨, InDesign
   기준).
3. (2026-08-25) LLM 상태 배지가 Standby에 고정되던 문제 →
   `check_ollama_health` 자동 헬스체크 추가로 해결(InDesign 기준).
4. Word는 브라우저 샌드박스 환경(WebView2)이라 InDesign(ExtendScript)과
   호출 경로가 다를 수 있음 — 위 수정들이 Word 쪽 경로에도 동일하게
   적용됐는지는 미검증.

## 질문
1. Word taskpane에서 QA 분석 파이프라인이 실제로 어떤 IPC 커맨드
   체인(`sendParagraphPayload` → 서버 → `analyze_paragraph` 등)을 타는지
   InDesign 경로와 비교해서 다른 점이 있는지 확인 부탁.
2. 위 참고 사고 1~3과 동일/유사한 원인(커맨드 미등록, 모델 재동기화 실패,
   헬스체크 배지 고정)이 Word 쪽에서도 재현될 수 있는 구조인지 코드로
   확인 부탁.
3. "동작 안 함"이 구체적으로 어떤 증상일 가능성이 높은지(카드 자체가
   안 뜨는지 / 뜨는데 내용이 이상한지 / 배지가 오프라인으로 뜨는지 등)
   코드 상에서 가장 유력한 실패 지점 후보를 우선순위로 나열 부탁 —
   사용자에게 재현 확인을 요청할 때 한 번에 여러 후보를 동시에 배제할
   수 있는 진단 방법(가능하면 로그 삽입 지점)도 같이 제시해 주세요.

코드 수정은 하지 말고 분석/원인 후보 제시만 부탁합니다.
