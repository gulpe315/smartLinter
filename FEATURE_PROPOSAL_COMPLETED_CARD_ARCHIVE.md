# 기능 제안 검토 요청: 완료된 QA 카드 숨김 + 재확인

## 사용자 제안 (2026-08-25)

[적용]으로 치환이 완료된 QA 카드를 목록에서 완전히 삭제하지 말고, "완료" 상태로 목록에서
숨겨뒀다가 "완료 보이기" 토글을 누르면 다시 꺼내서 확인하거나 재수정도 할 수 있게 하자는 제안.

배경: 방금 다른 버그 수정으로 `qaStore`에 `pendingCommands`(commandId -> cardId/paragraphId/
baseHash) 레지스트리가 생겼고, InDesign 쪽도 이어서 `command.paragraphId`로 실제 문단을 안정적으로
찾는 기능(현재 진행 중, 아직 미완료)을 추가할 예정 — paragraphId만 있으면 이미 완료된 카드라도
나중에 그 문단으로 다시 찾아가 재수정할 길이 열림.

## 현재 상태 (참고)

- 지금은 카드가 `SUCCESS`로 치환 완료되면 상태만 바뀌고 목록에는 그대로 남아있음(별도의
  "완료 아카이브"나 숨김 처리 없음). `dismissedCards`(무시한 카드)는 이미 별도 배열로 분리돼 있어서
  비슷한 패턴을 완료 카드에도 적용할 수 있어 보임.
- 완료 카드의 영속성(앱 재시작 시 유지 여부)은 없음 — 이번 제안이 영속 저장까지 포함하는지는
  불명확, 검토 시 이 부분도 짚어주기 바람.

## 요청

이 제안을 실현 가능성, 잠재적 문제, 더 나은 대안이 있는지 검토해서 의견을 달라(아직 구현하지
말 것, 분석/제안만):

1. `dismissedCards`처럼 `completedCards` 배열을 따로 두는 방식이 적절한지, 아니면 기존 `cards`
   배열에 status 필터링만으로 처리하는 게 나은지.
2. "재수정"이 의미하는 바 — 완료된 카드를 다시 활성 카드로 되돌려서 새 치환 명령을 보내는 것이라면,
   이미 치환된 최신 문단 내용을 기준으로 다시 QA 재분석부터 해야 하는지, 아니면 원래 카드의
   원문/제안을 그대로 재적용 가능하게 할지.
3. InDesign 쪽 문단 위치 추적(진행 중인 paragraphId 기반 조회)과 어떻게 연결되어야 하는지 — 완료된
   카드를 다시 열었을 때 "위치 이동"으로 그 문단까지 정확히 갈 수 있어야 자연스러운 기능일지.
4. 백로그에 이미 있는 "수정 이력 캐시"(같은 오류 패턴 재발견 시 자동 제안) 기능과 겹치거나
   상호보완적인 부분이 있는지.
5. UI 상 우선순위 — 지금 대기 중인 백로그(위치 보기, 인라인 수정, 일괄 적용) 대비 이 기능을 어느
   순서에 넣는 게 합리적일지 의견 부탁.
