# Task: TM 패널 라운드2 수정 — 리뷰 결함 6건

`TASK_REQUEST_TM_PANEL_EDIT_AND_SAVE.md` 구현에 대해 Codex와 agy가 각각
독립 리뷰한 결과 결함 6건이 확인됐다. 두 리뷰는 상충하지 않으며 서로
다른 것을 잡았다. 아래를 전부 수정할 것.

**범위 제약은 이전과 동일**: `plugins/`, `src-tauri/` 절대 수정 금지.
프론트엔드(`src/`)만.

## 결함 1 — 화면과 실제 동작이 다름 (최우선)
`src/components/tm/TMMatchCard.tsx`에서 편집을 확정하고 편집모드를 빠져나오면
표시는 `candidate.target`(원본)인데 [적용]/[복사]/[TM 저장]은
`editedSuggestion`(편집본)을 쓴다. **사용자가 보는 번역과 실제 적용되는
번역이 다르다.**

수정: 비편집 표시도 `effectiveTarget`을 쓸 것. 또한 편집된 상태임을
사용자가 알 수 있도록 시각적 표시(예: 수정됨 뱃지)를 추가할 것.
키워드 하이라이트는 편집본에는 적용하지 말 것(원본에만 의미 있음).

## 결함 2 — 저장 게이트가 지시와 다름
`isFuzzyMatch`가 `candidate.matchMode !== 'keyword'`로 판정된다. 이러면
사용자가 수동 검색창으로 임의 텍스트를 검색한 결과에도 저장이 활성화되어,
**현재 문단과 무관한 번역이 TM에 저장**된다.

수정: `useTmStore`의 `searchMode`를 구독해 `searchMode === 'fuzzy'`로
판정할 것. 추가로 **수동 검색 상태도 차단**할 것 — `searchQuery`가 현재
문단 텍스트와 다르면 저장 비활성화(사용자가 직접 다른 걸 검색한 상태이므로).

## 결함 3 — 스토어를 구독하지 않고 읽음
`useTmStore.getState()` / `useBridgeStore.getState()`를 렌더 본문에서
직접 호출한다. 훅이 아니라 명령형 읽기라 **문단이 바뀌어도 리렌더가
걸리지 않아** `canSaveToTm` 판정이 stale해진다. TM 오염을 막으려는
게이트가 옛 문단 기준으로 판정되는 것이므로 치명적이다.

수정: `useTmStore((s) => ...)` / `useBridgeStore((s) => ...)` 선택자 훅으로
구독할 것.

## 결함 4 — 문장 판정 불안전 + 안전망 부재
`split(/[.!?\n]+/)`은 양방향으로 오판한다.
- false positive(저장을 과하게 막음): `1.5배`, `v2.0`, `U.S.A.`,
  `config.json`, `docs.google.com`, `'정말입니까?'라고 물었다`
- **false negative(오염을 허용함, 더 위험)**: 종결부호 없이 나열된
  여러 문장이 1개로 통과된다.

완벽한 문장 분리는 문장/TU 경계 작업(별도 과제)의 몫이므로 이번엔
**휴리스틱을 개선하되 그것에 의존하지 않는 안전망을 둔다.**

수정 두 가지를 모두 할 것:
1. 판정을 **종결부호 뒤에 공백/문자열끝이 오는 경우**로 좁힐 것.
   대상 부호에 `。`와 `…`도 포함. 이러면 `1.5배`/`config.json`처럼
   부호 뒤에 공백이 없는 경우는 문장 경계로 세지 않는다.
2. **저장 직전 확인 대화상자를 띄워 실제로 저장될 원문/번역 쌍 전문을
   보여줄 것.** 사용자가 눈으로 확인하고 저장하게 하는 것이 휴리스틱보다
   확실한 안전망이다. 기존 충돌 확인 대화상자와 중복되지 않게 통합할 것
   (충돌이 있으면 충돌 사실도 같은 대화상자에 함께 표시).

## 결함 5 — 편집 취소 시 저장됨 상태가 안 풀림
TM 저장 후 다시 연필을 눌러 취소(X)하면 `cancelEditingSuggestion`이
`setEditedSuggestion(null)`만 하고 `setIsTmSaved(false)`를 안 한다.
표시는 원본으로 돌아가는데 뱃지는 저장됨으로 남아 불일치한다.

수정: 취소 시에도 저장됨 상태를 해제할 것.

## 결함 6 — 테스트가 정정 사항을 검증하지 않음
현재 테스트는 303개가 통과하지만 위 결함들을 전혀 못 잡았다. 아래를
모두 추가할 것.
- 편집 확정 후 **표시되는 텍스트가 편집본**인지.
- `searchMode`가 keyword일 때 [TM 저장] disabled.
- `searchQuery`가 현재 문단과 다를 때(수동 검색) [TM 저장] disabled.
- 문단이 복수 문장일 때 disabled, 그리고 `1.5배`처럼 부호 뒤 공백이
  없는 단일 문장은 **활성화**되는지(결함 4-1 검증).
- target이 공백뿐일 때 [적용]/[TM 저장] disabled.
- TM 저장 후 재편집 시, 그리고 편집 취소 시 저장됨 상태가 풀리는지.
- 문단이 바뀌면 편집 상태가 초기화되는지.
- `tmStore.applyMatch`에 `overrideTarget`을 넘겼을 때 **원본 candidate로
  카드 상태(applying/applied)가 정상 갱신**되는지 스토어 레벨 테스트.

## 완료 조건
`npm test`, `npx vitest run`, `npm run build` 전부 통과.
