# 기능 제안 검토 요청: 포커스 기반 대신 전체 문서 실시간 QA 스캔

## 사용자 제안 (2026-08-25)

실제 사용 중 발견한 문제: InDesign에서 자동으로 감지된 QA 카드("일오일" -> "일요일" 오탈자)와,
사용자가 하단 "AI 커맨드 채팅"에서 "한글 맞춤법과 띄어쓰기 규칙에 맞게 교정해줘"를 실행해서 나온
결과(같은 문단, 같은 오탈자를 gemma2:latest로 즉시 교정·적용함, "에디터 문서에 무손실 치환됨"으로
표시됨)가 **서로 다른 두 파이프라인이라 연동되지 않고**, AI 커맨드로 이미 고쳐졌는데도 자동 감지
QA 카드는 여전히 [적용]/[위치 보기] 버튼이 활성 상태인 채로 pending으로 남아있음.

이걸 보고 사용자가 제안: "지금처럼 InDesign 포커스가 있는 문단(활성 선택)만 검토하는 방식 대신,
백그라운드에서 계속 문서 전체를 대상으로 QA 카드를 실시간으로 리스트업해주는 게 낫지 않냐. 기존의
수동 '전체 검사' 버튼은 그대로 유지하면서."

## 참고: 관련된 기존 이슈/백로그

- 방금 별도로 진단·수정 위임한 "해결된 카드가 자동으로 안 사라지는 버그"(Task F,
  BUG_ANALYSIS3_CODEX.md / BUG_ANALYSIS3_AGY.md)가 이 스크린샷의 증상 일부(자동 카드가 여전히
  살아있는 것)를 해결할 가능성이 높음 — Task F의 "직접 수정 감지"는 최신 텔레메트리 기준으로
  판단하므로 수정 주체(사용자 직접 타이핑이든, AI 커맨드 적용이든)와 무관하게 작동해야 함. 다만
  이건 "이미 만들어진 카드가 늦게라도 정리되는 것"이지, "애초에 문서 전체를 실시간으로 커버하는 것"
  과는 다른 문제.
- `ORCHESTRATOR_STATUS.md` 백로그에 이미 `start_batch_scan`/`abort_batch_scan`(문서 전체 일괄
  스캔) 항목이 있음 — "InDesign/Word 어느 쪽에도 '전체 문단 열거' 기능 자체가 없어서 별도 설계
  필요, 아직 미착수"로 기록돼 있었음. 이번 제안은 이 백로그 항목과 사실상 같은 방향이거나, 그보다
  더 나아가 "일회성 전체 스캔"이 아니라 "상시 실시간 전체 감시"를 요구하는 것으로 보임.
- 현재 구조(참고): InDesign daemon의 `onIdleTick`이 현재 활성 선택/커서 위치의 문단만 감지해서
  `new-paragraph-detected`로 보내고, 프론트엔드가 1초 디바운스 후 그 문단 하나만
  `analyzeParagraph`(LLM 호출)함. "전체 문서 실시간 스캔"이 되려면 이 감지 범위 자체를 활성 선택이
  아니라 문서의 모든 Story/문단으로 넓혀야 함.

## 요청

이 제안을 실현 가능성, 잠재적 문제, 더 나은 대안이 있는지 검토해서 의견을 달라(아직 구현하지
말 것, 분석/제안만):

1. "포커스 기반(활성 선택만 감지)"을 "전체 문서 상시 감시"로 바꾸는 것이 구조적으로 타당한지. 특히
   LLM 호출 비용/지연(문서가 크면 문단이 수십~수백 개일 수 있음), Ollama 로컬 서버 부하, 배터리/CPU
   사용량 측면에서 우려되는 부분이 있는지.
2. 기존 백로그의 `start_batch_scan`/`abort_batch_scan`(일회성 전체 스캔, 사용자가 버튼으로 트리거)
   과 이번 제안(상시 실시간 전체 감시)을 어떻게 관계짓는 게 좋을지 — 상시 감시가 정답이라면
   일회성 버튼은 필요 없어지는지, 아니면 사용자가 명시했듯 "전체 검사 버튼은 유지하면서 상시 감시도
   추가"하는 두 기능 병행이 맞는지.
3. 상시 전체 감시를 한다면, 실용적인 절충안이 있는지(예: 전체 문서를 한 번에 다 스캔하는 게 아니라
   유휴 시간에 점진적으로 문단을 순회하며 백그라운드에서 조금씩 스캔 큐에 넣는 방식, 이미 스캔한
   문단은 해시가 안 바뀌면 재스캔 생략, 우선순위 큐 등).
4. Task F(방금 위임한 "직접 수정 감지")가 이번 스크린샷의 "AI 커맨드로 고친 카드가 자동 카드와
   연동 안 됨" 문제를 실제로 해결할지, 아니면 별도 조치가 더 필요한지도 같이 짚어달라.
5. 우선순위 — 지금 밀려있는 다른 백로그(위치 보기는 완료, 인라인 수정, 일괄 적용, 완료 카드
   아카이브)와 비교해서 이 기능을 언제 착수하는 게 합리적일지 의견 부탁.
