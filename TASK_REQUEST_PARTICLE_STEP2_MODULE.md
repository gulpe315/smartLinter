# Task: 조사 호응 — Step 2 (`particle.pronoun` 모듈 + 보호막, Part A)

`CODEX_DESIGN_PARTICLE_WHITELIST_AND_KIWI_SPIKE.md`의 "Part A — `particle.pronoun`"
(A.1~A.4)을 구현합니다. Step 1(커밋 `d1e7fc2`)에서 `QaSuggestion`/`suggestions`
스키마를 이미 추가했지만, **이번 단계는 그걸 쓰지 않습니다** — `particle.pronoun`
규칙은 표(A.3)의 각 항목이 정확히 하나의 교정만 내므로 단일 `suggested_segment`만
채우면 됩니다. 재설계/재자문 불필요 — 아래와 설계 문서 그대로 구현하면 됩니다.

## 중요: 이번 단계에서는 라이브 파이프라인에 연결하지 마세요

`deterministic_qa::detect()`가 지금 이 새 모듈을 호출하도록 **연결하지 마세요.**
모듈은 완성하고 충분히 테스트하되, `detect()`의 리턴값에는 아직 반영되지 않는
**미사용(dormant) 상태로 커밋**합니다(이전 `getLiveParagraphSnapshots` 배치
primitive를 Step 3에서 만들고 Step 4까지 호출부를 안 붙인 것과 같은 패턴).

이유: 설계 문서 A.3이 명시하듯 대명사 `그`는 `그은`(긋다의 관형형, "그은 선"처럼
실제 동사 활용형)과 **표면 문자열이 완전히 동일**해서, 지금 설계된 경계/보호막
메커니즘으로는 이 둘을 구분할 방법이 없습니다. 설계 자체도 "스템별 코퍼스에서
클린 텍스트 오탐이 하나라도 나오면 그 스템은 파일럿에서 제외된다"는 별도 검증
게이트(Part A.5)를 전제로 하고 있고, `그`는 그 게이트를 통과 못 할 가능성이
실제로 있다고 설계 문서 스스로 명시합니다. 코퍼스 검증(A.5, 스템당 3,000+1,000건)은
이번 코드 구현 작업과 분리된 별도 태스크이므로, 검증 전에 실제 QA 카드에 이
규칙이 노출되면 사용자가 실제로 오탐(예: "그은 선을 따라 걸었다" → "그는 선을
따라 걸었다"로 잘못 "교정")을 볼 위험이 있습니다. 그래서 이번 단계는 **모듈을
만들고 테스트로 검증하는 것까지**만 하고, `detect()`에 실제로 연결해서 사용자에게
노출하는 건 코퍼스 검증 이후의 별도 단계로 미룹니다.

## 구현할 것

### 1. 새 파일 `src-tauri/src/deterministic_qa/particle_pronoun.rs`

기존 `Dictionary`/`dictionary_issue()`/카테고리 루프/마커 규칙/`protected_spans()`/
`has_leading_boundary()`/`has_trailing_boundary()`/`has_strict_trailing_boundary()`/
`utf16_offset()`/`overlaps()`는 전혀 건드리지 마세요. `dictionary.json`에도
아무것도 추가하지 마세요 — 이 30개 매핑은 코드에 직접 데이터로 박아넣습니다
(설계 문서 표현 그대로: "code-owned rule family", JSON 티어/메시지/provenance/
confidence를 소비하지 않음).

모듈의 공개 계약(설계 A.1 그대로):

```rust
pub(crate) fn detect_particle_pronoun(
    text: &str,
    inherited_protected: &[(usize, usize)],
    options: &ParticlePronounOptions,
) -> Vec<QaIssue>;

pub(crate) struct ParticlePronounOptions<'a> {
    /// Exact full tokens supplied by an integration/document glossary which
    /// must never be grammatical-normalized. Empty for now — no caller wires
    /// a real glossary source yet.
    pub protected_literals: &'a std::collections::HashSet<String>,
}
```

`inherited_protected`는 (아직 호출부가 없으므로) 테스트에서 빈 슬라이스 또는
`deterministic_qa::mod`의 기존 `protected_spans()` 결과를 직접 넘겨 검증하세요.
이 값과 모듈 자체의 `particle_protected_spans()` 결과를 합집합으로 취급합니다
(기존 일반 보호막 정책을 넓히는 게 아니라, 배제 범위를 더하는 합성입니다).

매칭 대상 표면형(전체 "어간+오류조사" 문자열)마다 다음을 전부 만족해야만
이슈를 냅니다:

1. 상속받은 보호막 또는 조사 전용 보호막과 겹치지 않을 것;
2. `has_leading_boundary(text, start)`;
3. **엄격한** 후행 경계 — `has_strict_trailing_boundary(text, end)`를 쓰세요
   (기존 `has_trailing_boundary()`의 "조사 연속 예외"는 절대 쓰지 마세요 —
   그 예외는 이 규칙엔 적용되면 안 됩니다);
4. 매칭된 전체 표면형이 `protected_literals`에 없을 것;
5. NFC 완성형 한글로만 구성될 것(고정 표에서는 항상 참이지만, 미래 방어용
   assert로 남겨두세요).

오프셋은 기존 `utf16_offset()` 헬퍼를 그대로 재사용하세요. 교정은 항상
"어간+오류조사" 전체를 대체합니다 — 긴 단어 내부의 접미부만 스캔하거나,
어간을 추론하거나, 이웃 품사를 조사하거나, 라틴/한자/숫자/이모지/경로/혼합
스크립트 토큰에 매칭하지 마세요.

### 2. 조사 전용 보호막 — `particle_protected_spans(text, options)` (설계 A.2)

선형 스캔으로 바이트 스팬을 반환하세요. 겹치거나 인접한 스팬은 `overlaps()`
호출 전에 합치세요. 여는 델리미터에 대응하는 닫는 델리미터가 없으면, 그 여는
지점부터 물리적 줄 끝(또는 텍스트 끝)까지 통째로 보호합니다(닫히지 않은 인용은
내용을 들여다볼 이유가 없다는 뜻 — 조사 규칙 한정으로 의도적으로 fail-closed).

**인용부호/예시:**
- 한글/CJK 인용부호(`""`, `''`, `「」`, `『』`, `《》`, `〈〉`): 서로 다른
  델리미터의 중첩은 허용, 같은 델리미터의 중첩은 추론하지 않음(스택 기반).
- ASCII 인용부호(`"..."`, `'...'`): 토큰/공백/구두점 경계에 있는 델리미터만
  여는 것으로 인식하고, 같은 줄에서 그 뒤에 오는 매칭 델리미터를 찾음(식별자
  안의 아포스트로피를 인용부호로 오인하지 않기 위함). 같은 줄 내에서만
  비중첩 쌍으로 취급. 이스케이프(`\"`, `\'`)는 ASCII 쌍을 닫지 않음.
- 기존 `protected_spans()`가 처리하는 백틱/`{{...}}`/HTML형 태그/URL은
  그대로 상속받은 보호막에 이미 포함되므로 이 함수에서 중복 구현하지 마세요.

추가로, 트림한 줄 내용이 `예`, `예시`, `예문`, `입력`, `출력`, `문법`, `표기`,
`code`, `example` 중 하나로 시작하고 그 뒤에 `:`, `：`, `-`, `—` 중 하나가
오면, 그 마커 이후 텍스트를 물리적 줄 끝까지 보호하세요(따옴표 없는 문서/학습
예시도 보호하되, "예시"라는 단어가 그냥 포함된 평범한 문장은 보호하지 않음).

**식별자/제목:** 한글은 의미 있는 대소문자 신호가 없으므로 케이스 휴리스틱을
만들지 마세요. 다음 두 경로만 사용합니다:

1. **메타데이터 없이 인식되는 구문적 맥락:** 물리적 줄 전체를 보호 —
   Markdown/아웃라인 제목(`#`~`######` + 공백), 전체가 라벨인 줄(`토큰:` 또는
   `토큰：`만 있는 줄), 대괄호/괄호/중괄호 필드 라벨(`[토큰]`, `(토큰)`,
   `{토큰}`), `|`로만 구분되는 표 셀 값, 아웃라인 항목(`-`/`*`/`+`/`N.`/`N)`
   + 토큰 하나). 여기서 `token`은 매칭된 전체 표면형이고 주변에 공백만
   있어야 합니다.
2. **명시적 보호 리터럴 어휘(호출 계층이 나중에 문서 용어집/제목 메타데이터를
   제공하면 사용):** NFC 완전 토큰만 정확히 매칭(부분 문자열 금지). 지금은
   빈 집합을 넘기지만, 이 확장 지점(`protected_literals`)은 반드시 노출해
   두세요.

또한 ASCII 문자/숫자와 `._:/\@#-` 중 하나 이상을 포함하는 공백으로 구분된
토큰, `--`나 `/`로 시작하는 토큰도 조사 규칙 전용 식별자 스팬으로 취급하세요
(30개 매핑이 순한글이라 엄격한 경계 덕에 이 안에서 매칭될 일은 사실상 없지만,
안전 정책을 명시적으로 해두는 차원입니다).

### 3. 정확한 파일럿 매핑 표 (설계 A.3, 그대로 하드코딩)

10개 어간 × 3개 조사쌍(은/는, 이/가, 을/를) = 30개 매핑. 받침은 어간의 마지막
음절 받침 기준입니다.

| 어간 | 받침 | 은/는 (오류→교정) | 이/가 (오류→교정) | 을/를 (오류→교정) |
| --- | --- | --- | --- | --- |
| 그들 | 있음(ㄹ) | 그들는→그들은 | 그들가→그들이 | 그들를→그들을 |
| 우리 | 없음 | 우리은→우리는 | 우리이→우리가 | 우리을→우리를 |
| 당신 | 있음(ㄴ) | 당신는→당신은 | 당신가→당신이 | 당신를→당신을 |
| 그 | 없음 | 그은→그는 | 그이→그가 | 그을→그를 |
| 그녀 | 없음 | 그녀은→그녀는 | 그녀이→그녀가 | 그녀을→그녀를 |
| 이것 | 있음(ㅅ) | 이것는→이것은 | 이것가→이것이 | 이것를→이것을 |
| 그것 | 있음(ㅅ) | 그것는→그것은 | 그것가→그것이 | 그것를→그것을 |
| 저것 | 있음(ㅅ) | 저것는→저것은 | 저것가→저것이 | 저것를→저것을 |
| 누구 | 없음 | 누구은→누구는 | 누구이→누구가 | 누구을→누구를 |
| 무엇 | 있음(ㅅ) | 무엇는→무엇은 | 무엇가→무엇이 | 무엇를→무엇을 |

`으로/로`, `과/와` 매핑은 이번 설계에 포함되지 않습니다(Kiwi 단계로 연기).
`그` 항목도 위 표 그대로 코드에 포함하세요 — 실제 라이브 노출은 안 하니(위
"연결하지 마세요" 참고) 지금 배제할 필요 없고, 오히려 다음 단계(코퍼스 검증)
때 이 코드가 있어야 테스트할 수 있습니다.

### 4. 이슈 생성 — `particle_issue()` 전용 생성자 (설계 A.4)

`dictionary_issue()`를 재사용하지 말고 새 생성자를 만드세요. 고정 필드:

```text
category:    "particle.pronoun"
rule_id:     "particle.pronoun.v1.<어간>.<오류조사>"   (예: particle.pronoun.v1.그들.는)
provenance:  "deterministic:particle-whitelist-v1"
severity:    QaSeverity::Medium
confidence:  0.90
suggestions: None   (Step 1에서 만든 필드지만 이번 단계는 안 씀)
```

`reason`은 "어간이 받침이 있으므로/없으므로 어떤 조사가 맞는지"를 설명하는
문장으로 만드세요(설계 문서 예시 참고, 정확한 한국어 문구는 자연스럽게
작성해도 됩니다 — 다만 **ExtendScript 파일이 아니라 순수 Rust 파일이므로
한글 리터럴을 직접 써도 됩니다**, 이건 `.jsx` 비ASCII 금지 규칙과 무관).
`0.90`은 이 결정론적 규칙의 정책값이지 개별 케이스의 확신도가 아니라는 점을
docstring이나 주석으로 남기세요 — 나중에 코퍼스 검증 통과 후에만 조정 가능.

### 5. `deterministic_qa/mod.rs` 변경 (최소한만)

`mod particle_pronoun;` 선언만 추가하세요. **`detect()` 함수 본문은 위
"연결하지 마세요" 섹션대로 건드리지 마세요** — `pub(crate)` 가시성이면 모듈
바깥(테스트 등)에서 `detect_particle_pronoun`을 직접 호출할 수 있으니 충분합니다.

## 테스트

전체 코퍼스(A.5, 스템당 3,000+1,000건)는 이번 단계 범위가 아닙니다. 대신
`particle_pronoun.rs` 안에 다음을 확인하는 단위 테스트를 작성하세요:

- 30개 매핑 전부: 평범한 한국어 문장 안에서 정확히 매칭되고 올바른 교정을
  내는지(어간 하나씩 대표 예문 1개면 충분, 30개 전부 커버).
- 올바른 조사가 이미 쓰인 문장(예: "그들은")에는 이슈가 안 뜨는지.
- 인용부호로 감싼 경우(`"그들는"`, `「그들는」`), 미완성 인용(여는 인용부호만
  있고 안 닫힘) 이후 줄 끝까지, `예: 그들는` 같은 예시 마커 뒤, 마크다운
  제목/라벨/괄호필드/표 셀/아웃라인 항목 안에 있는 경우 전부 이슈가 안 뜨는지.
- 식별자형 토큰(예: `그들는.txt`, `--그들는`) 안에서는 애초에 순한글 30개
  표면형만으로는 강한 경계 때문에 매칭 안 되는 게 정상이지만, 혹시 모르니
  대표 케이스 1~2개 정도 방어적으로 테스트.
- **`그` 항목에 대한 회귀 문서화 테스트:** "그은 선을 따라 걸었다"(긋다
  활용형) 같은 문장에 대해, 지금 설계로는 오탐이 날 수 있다는 걸 아는 채로
  현재 동작을 그대로 기록하는 테스트를 남기세요(코드가 이걸 고치는 게 아니라,
  다음 코퍼스 검증 단계에서 이 스템의 운명을 결정한다는 걸 문서화하는
  목적입니다 — 테스트 이름에 `그`의 알려진 한계임을 명시).
- `protected_literals`에 있는 표면형은 매칭에서 제외되는지(빈 집합이 아닌
  경우로 최소 1개 테스트).

## 하지 말 것 (범위 이탈 방지)

- `deterministic_qa::detect()`가 이 모듈을 호출하도록 연결 금지(위 섹션 참고).
- `merge()` 변경 금지(Part B.2, 다음 단계).
- `QACardItem.tsx` 등 UI 변경 금지(Part B.3, 다음 단계).
- `dictionary.json`에 아무것도 추가하지 마세요.
- `그` 스템을 표에서 빼거나 넣거나 하는 임의 판단 금지 — 설계 문서 그대로
  10개 전부 포함.
- 기존 `has_trailing_boundary()`(조사 연속 허용 버전)를 이 신규 규칙에
  쓰지 마세요 — 반드시 `has_strict_trailing_boundary()`만 쓰세요.

## 완료 후

`cargo test`, `npm test`, `npm run test:ui`, `npm run build` 전부 통과해야
합니다(TS 쪽은 이번 단계에서 안 건드리므로 회귀만 없으면 됨). 브릿지 서버
`smart-linter.exe`가 실행 중이면 `cargo test`가 기본 target을 못 씁니다 —
`CARGO_TARGET_DIR`을 임시 경로로 지정해서 실행하거나, 필요하면 그냥
결과만 보고해주세요(Claude가 검증 시 처리합니다). 이번 단계는 아직 아무
데도 연결 안 됐으므로 InDesign 라이브 검증도, 서버 재기동도 필요 없습니다.
