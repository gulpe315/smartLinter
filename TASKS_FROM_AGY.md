# TASKS_FROM_AGY (스파이크 구현 태스크 목록)

본 문서는 `SmartLinter_Plan.md`의 6번 섹션 "프로토타입(Spike) 검증 계획"을 바탕으로 작성된 구현 태스크 목록입니다. 세 가지 스파이크는 서로 독립적으로 구성되어 병렬 진행이 가능합니다.

---

## Task 1: 포맷 보존 텍스트 치환 및 롤백 Spike (Word & InDesign)

*   **목표:** Word와 InDesign 플랫폼에서 네이티브 에디터의 원본 서식을 파괴하지 않고 텍스트를 치환(Multi-hunk 역순 치환)할 수 있는지 확인하고, 실패/오류 상황에서 원본 상태로 안전하게 롤백하는 메커니즘을 실증합니다. 특수 요소 존재 시의 오프셋 드리프트 현상 방어 로직을 검증합니다.
*   **설계 근거:** 
    *   `2. 런(Run) 단위 서식 보존 및 안정성 확보 전략` > `A. Multi-Hunk 역순 치환 (Reverse-order) 및 롤백 (안전망 UX)`
    *   `6. 프로토타입(Spike) 검증 계획` > `1. 포맷 보존 텍스트 치환 및 롤백 Spike`
*   **완료 조건(Acceptance Criteria):**
    *   각주, 하이퍼링크 등 특수 요소가 포함된 샘플 문서에서 부분 텍스트 치환 시 오프셋 드리프트(텍스트 위치 어긋남) 0건 발생 확인.
    *   **Word:** 치환 중 오류 발생을 시뮬레이션했을 때, 보상 트랜잭션(Compensating Transaction)을 통해 텍스트 복구가 100% 정상 작동함을 확인.
    *   **InDesign:** 오류 발생 시, `app.doScript(..., UndoModes.ENTIRE_SCRIPT)`를 통한 네이티브 원자적 롤백이 100% 정상 작동함을 확인.
*   **선행 조건/의존성:** 없음 (독립적 진행 가능)
*   **산출물:**
    *   Word용 PoC 스크립트 (역순 치환 로직 및 보상 트랜잭션 코드)
    *   InDesign용 PoC 스크립트 (역순 치환 로직 및 `UndoModes.ENTIRE_SCRIPT` 적용 코드)
    *   테스트 수행 결과 로그 및 요약 리포트
*   **설계상 불명확하거나 추가 결정이 필요한 부분 (열린 질문):**
    *   Word의 보상 트랜잭션 수행 중 사용자가 키보드 입력을 하거나 자체 Undo를 실행할 경우의 충돌 방지 정책(예: 작업 중 일시적 문서 잠금 허용 여부)은 어떻게 가져가야 할까요?
    *   오프셋 드리프트 방어를 위해 대상 '특수 요소'를 텍스트 내의 인라인 객체(각주, 링크)로만 한정할지, 아니면 표나 이미지 캡션 내부 텍스트까지 스파이크 범위에 포함할지 확인이 필요합니다.

---

## Task 2: 이벤트 루프 & Task Pane 공식 숨김 Spike

*   **목표:** Word와 InDesign 환경에서 플러그인/애드인이 사용자에게 노출되는 UI(Task Pane, Panel) 없이도 백그라운드에서 이벤트 루프를 유지하며 지속적으로 동작할 수 있는지 검증합니다.
*   **설계 근거:** 
    *   `1. 전체 시스템 아키텍처 (브릿지 패턴)` > `A. 통신병 (Bridge Plugin)`
    *   `6. 프로토타입(Spike) 검증 계획` > `2. 이벤트 루프 & Task Pane 공식 숨김 Spike`
*   **완료 조건(Acceptance Criteria):**
    *   **Word:** 매니페스트에 Shared Runtime(`lifetime: "long"`)을 설정하고 `Office.addin.hide()` API를 호출하여 Task Pane이 사라진 상태에서, 문서 변경 이벤트 리스너가 정상 작동하며 최소 10분간 이벤트 루프가 유지됨을 확인.
    *   **InDesign:** UXP 플러그인 패널을 숨기거나 닫았을 때, 백그라운드에서 실행되는 폴링(setInterval) 또는 문서 이벤트 리스너가 최소 10분간 중단 없이 유지됨을 확인.
*   **선행 조건/의존성:** 없음 (독립적 진행 가능)
*   **산출물:**
    *   Word Add-in 최소 PoC 세팅 (Manifest.xml, JS 스크립트)
    *   InDesign UXP 최소 PoC 세팅 (manifest.json, JS 스크립트)
    *   백그라운드 이벤트 감지 및 타이머 실행을 증명하는 콘솔(또는 파일) 타임스탬프 로그
*   **설계상 불명확하거나 추가 결정이 필요한 부분 (열린 질문):**
    *   InDesign UXP의 경우 패널을 완전히 "닫았을 때(Closed)"와 패널 그룹에서 보이지 않게 "숨겨졌을 때(Hidden)"의 라이프사이클이 다르게 동작할 수 있습니다. 닫았을 때 스크립트 실행이 완전히 강제 종료된다면, UI가 아예 없는 백그라운드 전용 플러그인(Command-only) 방식으로 선회해야 할 수 있는데, 이에 대한 대응 플랜을 바로 적용해도 될까요?

---

## Task 3: 로컬 LLM 지연시간(Latency) 벤치마크 Spike

*   **목표:** 대시보드와 LLM 간의 벤치마크 테스트를 통해 단일 문단 단위 품질 검수(QA) 처리 시 소요되는 실제 응답 속도를 측정하고, 최적화 기법의 효과를 실증합니다.
*   **설계 근거:** 
    *   `4. 로컬 하드웨어(VRAM/RAM) 다운 방지 최적화 전략`
    *   `5. 작업 플로우 (User Workflow)` > `3. 비동기 피드백 팝업`
    *   `6. 프로토타입(Spike) 검증 계획` > `3. 로컬 LLM 지연시간(Latency) 벤치마크 Spike`
*   **완료 조건(Acceptance Criteria):**
    *   로컬 LLM을 구동하고 약 50~100단어 분량의 단일 문단을 입력하여 QA JSON 결과를 반환받기까지의 시간(전체 응답 시간 및 TTFT)을 30회 이상 반복 측정하여 평균 및 p95(상위 95%) 지연시간을 산출.
    *   프롬프트 압축(No Samples & JSON Force) 적용 전과 후의 응답 속도 차이 비교 데이터 확보.
*   **선행 조건/의존성:** 없음 (Word/InDesign 연동 없이, 독립된 스크립트 환경이나 터미널에서 API 호출 방식으로 독립적 진행 가능)
*   **산출물:**
    *   응답 시간 측정용 자동화 스크립트 (Python 등)
    *   측정 결과 리포트 (평균/p95 응답 시간 및 프롬프트 최적화 전후 비교 분석 데이터)
*   **설계상 불명확하거나 추가 결정이 필요한 부분 (열린 질문):**
    *   벤치마크의 유효성을 높이기 위해, 실제 실무 환경을 가정한 "기준 하드웨어 사양(VRAM 용량, 칩셋 등)"과 테스트에 사용할 "권장 로컬 LLM 모델(예: Llama-3 8B, Gemma-2 9B 양자화 버전 등)"의 명확한 기준이 정해져 있나요?
