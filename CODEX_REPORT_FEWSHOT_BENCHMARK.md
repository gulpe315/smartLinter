# English profile few-shot benchmark

실행: 2026-08-26, `exaone3.5:7.8b`, 13개 케이스 × 3회 × 3 variants. 아래 수치는 새 `english_fewshot` variant의 39회 응답에서 집계했다. 시드 9개는 유지했고, 오류 시드 7개와 신규 오류 holdout 4개를 분리했다.

| 지표 | 결과 |
| --- | ---: |
| 시드 9개 재현율 (오류 시드 7개 × 3회) | **85.71%** (18/21), 기존 71.43% 대비 **+14.28%p** |
| holdout 4개 재현율 (4개 × 3회) | **100.00%** (12/12) |
| holdout 포함 전체 재현율 (오류 11개 × 3회) | **90.91%** (30/33) |
| `en_mono_clean` 오탐률 | **100.00%** (3/3) |
| `en_bilingual_clean` 오탐률 | **0.00%** (0/3) |
| 클린 2개 합산 오탐률 | **50.00%** (3/6) |
| JSON 유효율 | **100.00%** (39/39) |

세 시드 실패는 모두 `en_mono_missing_comma`의 3회 응답이다. `en_mono_clean`의 세 오탐은 모두 `the user's work email address`를 `their work email address`로 바꾸도록 한 스타일 제안이었다.

참고 성능 수치: 평균 프롬프트 평가 토큰은 459.62, 평균 지연은 12,005.20ms, p95 지연은 19,794.11ms였다. 따라서 실제 Ollama 계측 프롬프트 크기는 400 nominal을 초과했다.

판정은 본 보고서에서 내리지 않았다. 사전 정의된 기준과 위 수치를 대조해 검토하면 된다.
