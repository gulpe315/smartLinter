# 문장 단위 CAT 정합성 확대 설계자문 — 검토단위/카드단위 전환 + SDLTM + 인라인 태그

## 사용자의 명확한 재확인 (직전 SRX 자문의 "UI그룹핑만" 결론을 다시 열게 된 이유)
직전 자문(`AGY_ANSWER_SRX_SEGMENTATION_REEVALUATION.md`,
`CODEX_ANSWER_SRX_SEGMENTATION_REEVALUATION.md`)에서 두 분 다 "SRX는
UI 시각적 그룹핑 경계 계산에만 좁게 쓰고, LLM 분석/카드생성 단위는
문단으로 유지"를 권고했습니다.

사용자가 이 결론에 대해 명확히 재확인함:
> "기본적으로 캣툴은 문장 단위야. 문장 단위가 캣툴 방식이니까 우리
> 앱도 **문장 단위가 검토 단위이자 카드 발생단위**로 보자는 거였어."
> "TM을 불러와서 이용하게 된다면, **번역 후 TM에 저장하는 것도 동일하게
> 할 수 있고** 말이야."

즉 사용자의 원래 의도는 "UI에서만 예쁘게 묶어 보이기"가 아니라,
**이 프로젝트를 CAT 툴(Trados류)과 동일한 문장(세그먼트) 단위로
검토→적용→TM저장까지 일관되게 동작하는 도구로 만들자**는 것이었습니다.
이건 두 분이 직전에 "리스크가 크다"며 반대한 **바로 그 전면 개편**을
사용자가 의도적으로 원하고 있다는 뜻입니다.

**추가로 이번에 새로 나온 요구사항 2건:**
1. TMX 외에 **SDLTM**(SDL/RWS Trados Studio의 TM 저장 포맷, TMX와
   달리 XML이 아니라 **SQLite 기반 바이너리 DB**)도 불러올 수 있어야
   함.
2. **문장 내부 인라인 태그**(예: 굵게/기울임 등 서식 태그, TMX의
   `<bpt>`/`<ept>`/`<ph>`/`<it>`류)를 세그멘테이션·분석·치환 전 과정에서
   보존해야 함.

## 요청
직전 자문을 그대로 반복하지 말고, **사용자가 명시적으로 재확인한
"검토/카드 단위 자체를 문장으로" + "TM 왕복 저장도 문장단위" + "SDLTM
지원" + "인라인 태그 보존"**을 전제로 처음부터 다시 스코핑해 주세요.
직전 자문의 반대 논거(문맥유실/비용폭증/상태머신붕괴)가 이 확장된
목표 하에서도 여전히 유효한 반대 사유인지, 아니면 목표 자체가 다르므로
다른 결론이 나오는지 재평가 부탁합니다.

### 질문 1: 검토/카드 단위를 문장으로 바꾸는 것 — 이번엔 "하지 말라"가
아니라 "어떻게 안전하게 할 것인가"로 다시 설계해 주세요
- 여전히 남는 반대 근거(LLM 컨텍스트 유실, 호출비용 3~5배, qaStore
  상태머신/부분적용/rollback 붕괴 위험)를 각각 어떻게 완화할 수 있는지
  구체적으로 검토해 주세요. 예:
  - LLM 호출은 문단 전체로 하되(컨텍스트 보존), **결과 파싱 이후에만**
    문장 경계로 QaIssue를 문장별로 그룹핑해서 카드를 생성하는 절충이
    직전 자문에서 이미 나왔음 — 이게 "카드 발생 단위 = 문장"이라는
    사용자 요구를 실제로 충족하는지, 아니면 사용자가 원하는 건 더
    근본적인 것(예: 문장별 독립 적용/롤백/TM저장 단위)인지 구분해서
    설계해 주세요.
  - "한 문장 카드 안에 여러 이슈, 부분 적용/부분 롤백" 문제를 어떻게
    설계하면 안전한지(예: 문장 카드 = 여러 이슈의 컨테이너, 개별
    이슈는 각자 상태를 갖되 UI는 문장 단위로 묶어서 보여주는 계층
    모델 — 직전 자문이 우려한 "상태머신 모순"을 실제로 피할 수 있는
    구체적 상태 전이 설계).
- SRX 등 세그멘테이션 엔진 도입은 이 전제 위에서 다시 필요성/우선순위를
  재평가해 주세요.

### 질문 2: TM 왕복 저장(불러오기+매칭+저장)을 문장 단위로 일관되게
- 현재 "TM에 저장" 기능(`QACardItem.tsx`의 `saveToTm`)이 문단 단위
  `tmReference.source`를 기준으로 저장하는데, 이걸 문장 단위로 바꾸려면
  무엇이 필요한지.
- 매칭(불러오기 후 검색)과 저장(교정 후 TM에 반영) 양쪽 다 같은 세그먼트
  경계 정의를 공유해야 하는데, 이게 구조적으로 안전하게 되는지.

### 질문 3: SDLTM 지원
- SDLTM은 SQLite 기반 바이너리 포맷으로 알고 있습니다 — 공식 스펙
  문서가 TMX처럼 공개돼 있는지, 아니면 커뮤니티 리버스엔지니어링
  자료에 의존해야 하는지 조사 부탁(가능하면 웹검색 활용, 라이선스/
  법적 리스크도 같이 확인 — RWS의 독점 포맷을 무단 파싱하는 것에
  대한 이용약관상 문제가 있는지도).
- 기존 `parse_tm_content`(TMX 파서)와 어떻게 공존시킬지(포맷 자동감지,
  공통 `TmEntry` 모델로 정규화 등) 스코핑.
- Rust에서 SQLite를 읽을 라이브러리(예: `rusqlite`) 사용 가능성과
  이 프로젝트의 기존 의존성과의 궁합.

### 질문 4: 인라인 태그 보존
- 현재 TMX 파서(`src-tauri/src/tm/tmx_parser.rs`)가 `<bpt>`/`<ept>`류
  인라인 태그를 어떻게 처리하고 있는지(보존/제거/무시) 코드로 확인.
- 세그멘테이션(문장 분리)이 태그 내부에서 잘못 끊어지지 않도록 태그를
  "보호 구간"으로 취급해야 하는데, 이게 SRX나 자체 세그멘터 설계와
  어떻게 통합되는지.
- InDesign/Word 양쪽에서 실제 치환 시 태그가 서식(굵게/기울임 등)으로
  올바르게 매핑/보존되는지, 아니면 이번 스코프에서는 "태그를 유지만
  하고 서식 매핑은 별도 과제"로 좁혀야 하는지.

## 우선순위/스코프 안내
이건 명백히 큰 설계 작업입니다. 답변은 다음을 반드시 포함해 주세요:
- 전체를 한 번에 하지 말고 어떤 순서(스파이크/단계)로 쪼개야 하는지.
- 각 단계별 리스크와 롤백 가능 지점.
- 이번 세션에 이미 완료된 Word 위치찾기 버그 수정(별도 트랙, 아직
  미착수) 등 기존 작업과 이 설계가 충돌하는 지점이 있는지.

코드 수정은 하지 말고 설계 분석만 부탁합니다.
