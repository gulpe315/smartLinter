# Task: 조사 호응 — 코퍼스 정밀도 스파이크 (Part A.5, 축소판)

`particle_pronoun` 모듈(Step 2, 커밋 `b6a4274`)이 이미 실제 Rust 코드로 존재하니,
이번엔 결정론적 오탈자사전 때처럼 별도 Python 프로토타입을 새로 만들 필요 없이
**실제 `detect_particle_pronoun()`를 직접 코퍼스로 테스트**합니다. 설계 문서
(`CODEX_DESIGN_PARTICLE_WHITELIST_AND_KIWI_SPIKE.md` Part A.5)가 요구하는
스템당 3,000+1,000+100건 규모는 사내 전용 도구 하나에 비현실적으로 크다고
판단해, **사용자 승인 하에 훨씬 작은 규모의 대표성 있는 코퍼스로 축소**합니다
(선례: 결정론적 오탈자사전 때 18 seeded + 18 오탐트랩 + 5 클린 문단 = 41건
규모로 정밀도 스파이크를 돌려 GO 판정을 받은 적 있음, `BACKLOG_DETERMINISTIC_SEQUENCE_TYPO_DICTIONARY.md`
"Precision spike" 절 참고 — 이번에도 같은 규모 감각으로 접근).

## 만들 것

### 1. 코퍼스 (직접 만들고 스스로 1차 검토)

새 파일 `src-tauri/tests/particle_pronoun_corpus_spike.rs` (또는 `#[cfg(test)]`
전용 모듈)에 데이터 배열로 구성하세요. 각 케이스는 입력 텍스트, 기대 판정
(`seeded_error`/`clean`/`protected`), 그리고 `seeded_error`면 기대 원문/교정
텍스트를 포함해야 합니다.

**포함해야 할 것 (설계 A.5의 정신을 축소 규모로 반영):**

- **30개 매핑 전부** — 각 매핑당 최소 2~3개의 서로 다른 자연스러운 한국어
  기술/업무 문장에서 오류 형태(seeded_error)와 올바른 형태(clean, 이슈 없어야
  함) 둘 다. 매핑당 문장을 재사용하지 말고 문맥을 다양화하세요(총
  30×(2~3 오류 + 1~2 정상) ≈ 100~150건 규모면 충분합니다).
- **보호 컨텍스트 전부(protected)** — 인용부호(CJK 4종+ASCII, 완성/미완성),
  예시 마커(`예:`, `example:` 등), 마크다운 제목/라벨/괄호필드/표 셀/목록
  항목, 식별자형 토큰. 각 카테고리마다 최소 2개 이상, 서로 다른 어간으로
  (전부 "그들는"만 쓰지 말고 다양하게) — 대략 20~30건.
- **"그" 스템 집중 검증(최우선 위험 항목)** — 설계 문서가 명시한 대로 "그은"이
  긋다(그리다/줄을 긋다)의 관형형과 충돌하는 게 핵심 위험입니다. 다음을
  **반드시** 포함하세요:
  - "그은" 형태가 실제로 긋다 활용형으로 쓰인 평범한 문장 최소 5개 이상
    (예: "선을 그은 다음", "밑줄을 그은 부분", "경계를 그은 지도" 등 서로
    다른 문맥) — 이건 **거짓양성(오탐)이어야 정상**이므로 결과 리포트에서
    "그" 스템의 실측 오탐률로 집계하세요(코드를 고쳐서 억지로 통과시키려
    하지 마세요 — 이번 스파이크의 목적이 바로 이 실측치를 얻는 것입니다).
  - "그이"(배우자를 뜻하는 실제 단어)가 쓰인 문장 최소 2개.
  - "그을"(그을다 활용형, 예: "볕에 그을은/그을린")이 쓰인 문장 최소 2개.
  - 위 세 함정과 별개로, "그"가 실제 대명사+오류조사로 쓰인 정상적인
    seeded_error 케이스도 포함(비교 기준용).
- **다른 위험 어간들의 실사용 유사 케이스** — "우리"(조직/팀명으로 쓰이는
  경우, 예: "우리은행"처럼 고유명사에 포함된 경우 vs 대명사로 쓰인 경우),
  "그것"/"이것"/"저것"이 UI 라벨이나 인용된 예시 안에 등장하는 경우 각
  2~3개씩.
- **클린 업무/기술 문단** — 위 30개 표면형을 전혀 포함하지 않는 평범한
  한국어 기술/업무 프로즈 문단 10~15개(다양한 주제: 회의록, 매뉴얼, 이메일,
  기획서 톤 등 섞어서) — 순수 오탐 여부만 확인.

총 규모는 대략 150~200건 정도로 예상되는데, 정확한 숫자보다 **위 카테고리를
빠짐없이 커버하는 게 더 중요**합니다.

### 2. 실행 및 리포트

각 케이스를 `detect_particle_pronoun(text, &[], &options)`(빈 `protected_literals`)에
통과시켜 실제 판정을 얻고, 기대값과 비교하세요. 결과를 스템별·조사쌍별로
집계해서 새 파일 `SPIKE_RESULTS_PARTICLE_PRONOUN_CORPUS.md`에 정리하세요
(형식은 기존 `SPIKE_RESULTS_TASK1~3.md`나 `BACKLOG_DETERMINISTIC_SEQUENCE_TYPO_DICTIONARY.md`의
"Precision spike" 절 스타일을 참고):

- 스템별 정밀도(clean/protected 케이스에서 오탐이 몇 건인지)와 재현율
  (seeded_error를 몇 건 정확히 잡았는지).
- **"그" 스템의 "그은/그이/그을" 함정 케이스에서 실제로 몇 건이 오탐(이슈
  발생)으로 나왔는지 정확한 수치** — 이게 이번 스파이크의 핵심 질문입니다.
- 다른 스템에서 예상 밖의 오탐/누락이 있었다면 원인 분석(경계 로직 문제인지,
  보호막 미비인지).
- 코드를 고치지 마세요 — 이번 단계는 순수 측정입니다. 만약 측정 중 명백한
  버그(예: 어떤 매핑이 아예 안 걸리는 오타 등급 결함)를 발견하면 고치지 말고
  리포트에 "발견된 버그"로 기록만 하고 저에게 보고해주세요.

### 3. 자체 1차 검토

코퍼스를 다 만든 뒤, 각 케이스의 기대값(특히 `protected`로 분류한 것들이
실제로 보호돼야 마땅한지, `seeded_error`가 실제로 자연스러운 한국어 문장인지)을
스스로 한 번 더 검토해서 이상한 케이스가 없는지 확인해주세요. 이 코퍼스는
다음 단계에서 agy에게 공유되어 독립적으로 재검증받을 예정이니, 억지스럽거나
비현실적인 예문은 피해주세요.

## 하지 말 것

- `particle_pronoun.rs`의 로직을 고치지 마세요(발견한 버그는 리포트만).
- `detect()`에 연결하지 마세요(여전히 dormant).
- `dictionary.json`이나 다른 카테고리 규칙을 건드리지 마세요.
- `cargo fmt`를 실행하지 마세요.

## 완료 후

`cargo test`로 새 스파이크 테스트 자체가 정상 실행되는지(테스트가 패닉 없이
전체 케이스를 순회하고 집계 결과를 출력하는지) 확인해주세요 — 이 테스트는
"통과/실패"가 아니라 "측정"이 목적이므로, 개별 케이스 불일치를 `assert`로
패닉시키지 말고 전부 수집해서 리포트에 반영하는 방식으로 짜주세요(기존
회귀 테스트들과는 성격이 다릅니다). `npm test`/`npm run test:ui`/`npm run build`는
이번 단계와 무관하니 안 돌려도 됩니다.
