# 자동 치환과 번역 모드/XLIFF 설계 분석

## 결론 요약

두 제안은 모두 실행 가능하지만, 현재 단계에서의 크기와 위험은 크게 다릅니다.

| 제안 | 독립 실행 가능성 | 현재 기준 규모 | 핵심 위험 | 권고 |
|---|---:|---:|---|---|
| TM 자동 치환 | 가능 | 중~대 | 원치 않는 문서 변경, 이벤트 되먹임, 안전한 일괄 취소 | 초기에는 `Exact + 명시적 실행`만 |
| 번역 모드 + XLIFF 사이드카 | 가능 | 대 | 문서 전체 수집·식별·세그먼트 상태·XLIFF 태그 | 자동 치환과 독립된 별도 트랙 |
| 원문+번역을 에디터에 병기 후 클린업 | 가능하나 비권고 | 매우 대 | 대량 문서 편집, 서식/구조 손실, 복구 실패 | Phase 0 결정과도 가장 충돌 큼 |

핵심 권고는 다음과 같습니다.

1. 자동 치환은 "현재 TM 적용 버튼을 자동으로 누르는 기능"으로 보면 안 됩니다. 문서 변경을 예약·검증·기록·되돌리는 별도 트랜잭션 기능으로 설계해야 합니다.
2. 자동 적용은 첫 릴리스에서 fuzzy 매치에 허용하면 안 됩니다. `100% exact`도 단독 조건으로는 부족하며, 명시적 자동 모드, 문장 단위, 최신 해시 검증, 확정된 TM 항목, 충돌 없음이 함께 필요합니다.
3. 번역 모드는 `qaStore`의 monolingual 계약을 bilingual로 되돌리는 방식이 아니라, 원문 문서를 그대로 유지하면서 별도 번역 세션과 XLIFF를 생성하는 "사이드카 CAT 워크스페이스"로 한정해야 합니다.
4. "클린업하여 번역 문서를 생성"은 XLIFF export보다 훨씬 큰 별도 제품 기능입니다. 특히 Word/InDesign의 원본 구조와 서식을 충실히 재현하려면 문서 전체 추출·구조 맵·서식/태그 보존·재삽입이 필요합니다. XLIFF export와 같은 릴리스에 묶지 않는 것이 안전합니다.

---

## 현재 구조에서 확인되는 전제

현재 에디터 텔레메트리의 `ParagraphPayload`는 문단 텍스트, 문단 ID, 해시, 문서 컨텍스트를 전달합니다. `source`는 정렬된 원문 세그먼트가 아니라 "문서명/경로 등의 컨텍스트 식별자"입니다. [`shared/protocol/types.ts`](/D:/smartLinter/shared/protocol/types.ts:28)

따라서 Phase 0의 결정은 여전히 타당합니다.

- QA는 문서에 현재 들어 있는 언어를 검사하는 monolingual 린터다.
- TM은 현재 문단을 검색 키로 쓰는 독립 지식베이스다.
- `qaStore`가 분석 요청 때 `source: ''`로 고정하는 것은 누락이 아니라 이 제품 모델에 맞춘 보호 장치다. [`qaStore.ts`](/D:/smartLinter/src/stores/qaStore.ts:844)

현재 TM 매칭은 에디터의 현재 문단 전체를 대상으로 자동 검색합니다. 커서 위치나 문장 범위는 전달되지 않으며, `tmStore.applyMatch()` 역시 현재 문단 전체를 후보 target으로 교체합니다. [`TMMatchPanel.tsx`](/D:/smartLinter/src/components/tm/TMMatchPanel.tsx:62) [`tmStore.ts`](/D:/smartLinter/src/stores/tmStore.ts:188)

문장 경계 공용 계약은 이미 있으므로, 문장 단위 검색·저장으로의 확장 기반은 존재합니다. 다만 "현재 커서가 놓인 문장"을 식별하고 그 범위만 바꾸는 기능은 아직 없습니다.

---

# 1. 자동 치환(자동 번역)

## 제품 판단

자동 치환은 현재의 "카드를 보고 사용자가 적용"하는 철학과 충돌합니다. 그렇다고 기능 자체가 부적절한 것은 아닙니다. 다만 제품 의미를 다음처럼 제한해야 합니다.

> 자동 번역이 아니라, 사용자가 사전에 승인한 확정 TM 번역의 제한적 자동 적용.

이름도 초기에는 `자동 번역`보다 `확정 TM 자동 적용` 또는 `Exact TM 자동 적용`이 더 정직합니다. "번역"은 fuzzy 후보나 MT까지 포함할 것처럼 보이지만, 첫 버전은 그러면 안 됩니다.

## 자동 적용 허용 조건

초기 자동 적용은 아래 조건을 모두 만족할 때만 허용하는 fail-closed 모델을 권고합니다.

| 조건 | 이유 |
|---|---|
| 사용자가 해당 문서 세션에서 명시적으로 자동 모드를 켬 | 백그라운드에서 문서가 바뀌는 놀람 방지 |
| 문장 단위 대상 | 문단 전체를 target으로 치환하면 문단 내 다른 문장을 잃을 위험 |
| TM source와 현재 문장이 정확히 동일 | fuzzy 매치 오적용 방지 |
| 후보가 하나뿐이며 target이 비어 있지 않음 | 동일 source의 충돌 번역 방지 |
| TM 항목이 "승인됨/신뢰됨" 상태 | 사용자가 저장한 미검수 TM을 자동 적용하지 않음 |
| 대상 문단이 잠겨 있지 않음 | 편집 불가 영역 보호 |
| 적용 직전 live snapshot과 base hash가 일치 | stale 상태에서의 오염 방지 |
| 문장 경계와 대상 범위가 최신 스냅샷에서도 동일 | 텍스트를 입력 중이거나 구조가 바뀐 경우 중단 |
| 인라인 태그/서식 보존 안전성이 검증된 문장만 | 현재 태그 보존 경로가 없으므로 초기에는 plain-text 범위만 |

`100% score`만으로는 충분하지 않습니다. 점수 계산이 정규화·공백·문장부호 처리를 포함할 수 있고, 같은 source가 여러 TM 항목에 있을 수도 있기 때문입니다. 따라서 "exact score"와 "유일한 승인 후보"는 분리해 판정해야 합니다.

fuzzy 자동 적용은 초기 릴리스에서 금지해야 합니다. 85%나 95%는 표시·정렬·일괄 검토 후보로는 유용하지만, 자동 문서 변경 기준으로는 부족합니다.

## 트리거 설계

권고 우선순위는 다음과 같습니다.

1. `자동 적용 후보 미리보기`
   - 문장을 떠날 때 exact 후보를 "자동 적용 가능"으로만 표시
   - 문서 변경 없음
   - 오탐, 중복 TM, 문장 경계 문제를 관찰

2. `선택 실행`
   - 사용자가 "이 문서에서 자동 적용 실행"을 누름
   - 미리보기 목록의 정확 매치만 순차 적용
   - 적용 전마다 live snapshot/해시 검증

3. `명시적 자동 모드`
   - 세션 한정 토글
   - 문단이 안정화된 뒤에만 적용
   - 타이핑 중, 선택 영역 변경 중, 새 자동 치환 결과 수신 중에는 보류

4. 장기적으로만 `문단 이탈 시 자동 적용`
   - "문단 이탈" 이벤트의 플랫폼별 의미가 먼저 확정되어야 함
   - Word/InDesign마다 이벤트 순서와 안정화 시점이 달라 별도 실측이 필요

"저장 시 일괄 적용"은 첫 버전 트리거로 부적절합니다. 저장 직전 대량 변경은 사용자가 무엇이 바뀌는지 확인하기 어렵고, 실패·중단·복구 범위도 커집니다.

## 텔레메트리 되먹임 방지

자동 치환 뒤 에디터는 변경된 문단을 다시 텔레메트리로 보낼 가능성이 큽니다. 이를 사용자가 방금 입력한 문단처럼 처리하면 다음 루프가 생깁니다.

```text
TM 자동 적용
  → 에디터 문단 변경
  → new-paragraph-detected
  → TM 재검색 / QA 재분석
  → 다시 자동 적용 후보
```

이를 막으려면 각 자동 적용에 다음 식별 정보를 가져야 합니다.

- `commandId`
- `paragraphId`
- `baseHash`
- `expectedHash`
- 대상 문장 범위와 적용 전/후 텍스트
- 적용 세션 ID
- `origin: auto_tm` 같은 변경 출처

대시보드는 `expectedHash`와 일치하는 후속 텔레메트리를 "내가 방금 적용한 결과 확인"으로 소비해야 하며, 신규 자동 적용 후보로 즉시 재진입시키면 안 됩니다. 일치하지 않으면 자동 세션을 중단하고 사용자 변경으로 간주해야 합니다.

다만 출처 필드를 텔레메트리에 그대로 추가하지 못하더라도, 명령 레지스트리와 `paragraphId + expectedHash`로 억제할 수 있습니다. 이 경우에는 명령 TTL, 중복 이벤트, 사용자의 즉시 Undo까지 포함해 엄격히 설계해야 합니다.

## 해시 기반 트랜잭션과의 관계

QA의 적용 경로는 다음 안전 장치를 갖습니다.

- 명령 발송 전에 pending command를 등록
- command ID로 비동기 결과를 상관관계 처리
- stale 결과를 별도 처리
- 롤백 가드로 결과를 후속 처리
- 다건 적용도 순차 실행

[`qaStore.ts`](/D:/smartLinter/src/stores/qaStore.ts:483) [`qaStore.ts`](/D:/smartLinter/src/stores/qaStore.ts:654)

반면 현재 TM 적용은 해시와 diff hunk를 만들지만, QA의 pending-command/rollback-guard 경로를 공유하지 않습니다. 성공 응답에 따라 카드 상태를 바꾸는 구조입니다. [`tmStore.ts`](/D:/smartLinter/src/stores/tmStore.ts:188)

따라서 자동 치환은 `tmStore.applyMatch()`를 반복 호출하는 방식으로 구현하면 안 됩니다. 다음 둘 중 하나가 필요합니다.

- QA와 TM이 함께 쓰는 범용 `ReplacementTransactionCoordinator`를 추출한다.
- 자동 TM 전용으로 QA와 동등한 pending command, stale 처리, rollback guard를 구현한다.

첫 번째가 장기적으로 낫습니다. 자동 TM 적용, QA 일괄 적용, 향후 번역 세션의 문장 적용이 모두 같은 안전 모델을 공유할 수 있기 때문입니다.

각 문장 적용 전에는 반드시 현재 문단 전체의 live snapshot을 읽고, 해당 문장 범위가 여전히 같은지 검증해야 합니다. 해시가 다르면 "부분적으로라도 적용"하지 말고 해당 항목을 skip/stale로 기록해야 합니다.

## 즉시 되돌리기 UX

필수입니다. 자동 적용은 사용자가 직접 누르지 않은 문서 변경이므로, 일반 Undo에만 맡기면 충분하지 않습니다.

권고 UX는 세션 단위입니다.

- 상단에 `이번 자동 적용: 12건 · 되돌리기` 배너
- 적용 항목 목록: 문서 위치, 원문, 적용 번역, 상태
- 각 항목별 `되돌리기`
- 세션 전체 `안전하게 일괄 되돌리기`
- 되돌릴 수 없는 항목은 이유를 명시: "적용 후 사용자가 수정하여 안전하게 되돌릴 수 없음"

일괄 되돌리기는 원래 적용의 역방향 명령을 순차 실행해야 합니다. 각 역적용 직전에도 현재 해시가 "자동 적용 직후 기대 해시"와 같은지 검사해야 합니다. 다르면 해당 항목은 중단해야 합니다. 이 규칙은 기존 계획의 pre-rollback hash check와 일치합니다. [`SmartLinter_Plan.md`](/D:/smartLinter/SmartLinter_Plan.md:28)

즉, "일괄 취소"는 강제 복구가 아니라 **검증된 항목만 되돌리는 best-effort 작업**이어야 합니다.

## 단계별 권고

| 단계 | 범위 | Release gate | Rollback/중단 기준 |
|---|---|---|---|
| A. 관찰 스파이크 | 문장 exact 후보를 표시만 함 | 문장 경계·TM 후보 일관성 확인 | 문서 변경 없음 |
| B. 수동 일괄 적용 | 사용자가 선택한 exact 후보를 순차 적용 | 해시/stale/중복 이벤트 테스트 통과 | 첫 stale 또는 실패 시 해당 항목 중단 |
| C. 세션 로그·되돌리기 | 자동 적용 원장과 개별/일괄 복구 | Word/InDesign에서 사용자 수정 후 복구 실패가 안전하게 abort됨 | 해시 불일치면 복구 금지 |
| D. 명시적 자동 모드 | 세션 한정, exact·승인 TM만 | 텔레메트리 억제와 중복 처리 실측 통과 | 예상 밖 텔레메트리/충돌 시 모드 자동 중지 |
| E. 문단 이탈 자동화 | 안정화 이벤트 기반 | 호스트별 이벤트 순서·재기동 검증 | 안정화 신호 불명확 시 적용 보류 |

현재 우선순위라면 A와 B만으로도 상당한 사용성 가치를 얻을 수 있습니다. D는 문서 변경 위험이 성격적으로 달라지는 경계이므로, 라이브 Word/InDesign 검증 전에는 출시하지 않는 것이 맞습니다.

---

# 2. 번역 모드와 XLIFF

## Phase 0 결정과의 관계

### 피해야 할 방향: 내부 계약을 bilingual로 되돌리기

"번역 모드"를 이유로 `ParagraphPayload.source`에 원문 세그먼트를 넣고, QA 파이프라인도 source/target을 전제로 바꾸는 방향은 Phase 0 결정을 사실상 뒤집습니다.

이는 다음을 모두 재설계하게 됩니다.

- 에디터에서 원문과 번역문을 어떻게 동기화할지
- 문서의 어느 쪽이 진실의 원천인지
- QA가 원문을 검사하는지 번역문을 검사하는지
- TM source/target 방향과 문서 언어의 관계
- 문단/문장 ID, 변경 추적, 충돌과 롤백

이 방향은 "번역 모드 추가"가 아니라 별도의 bilingual editor/CAT 제품을 만드는 일입니다. 권고하지 않습니다.

### 권고 방향: 번역 세션과 XLIFF를 파생 산출물로 두기

사용자 의도는 "에디터에 열린 원문을 TM으로 번역하고 CAT처럼 관리하며, 필요하면 이중언어 파일과 번역 결과물을 얻고 싶다"에 더 가까워 보입니다.

따라서 다음 구조가 가장 안전합니다.

```text
Word / InDesign 원문 문서
        │
        ├─ QA: monolingual 검사 (현행 유지)
        ├─ TM: 원문 문장 → 번역 제안
        │
        └─ 번역 세션 저장소
             ├─ source 문장
             ├─ target 초안/확정본
             ├─ 문서·문단·문장 식별자
             ├─ 상태, 해시, TM 출처
             └─ XLIFF export
```

이 모델에서는 XLIFF가 내부 canonical contract가 아닙니다. 사용자가 완료한 번역 세션에서 생성되는 파생 산출물입니다. 따라서 Phase 0의 QA monolingual 계약과 공존합니다.

## 번역 모드의 최소 정의

번역 모드는 단순 UI 토글이 아니라 최소한 다음 상태를 갖는 세션입니다.

- 번역할 문서 세션 ID와 문서 메타데이터
- source/target 언어
- 문단 및 문장별 안정 식별자
- source 텍스트와 캡처 당시 해시
- target 초안, 확정본, 검수 상태
- TM 후보와 실제 채택 출처
- 사용자 수정 이력과 충돌 상태
- 세그먼트 순서 및 최소한의 구조 정보
- 세션 범위: "문서 전체", "선택한 문단", "현재까지 수집한 문장" 중 무엇인지

여기서 가장 큰 신규 요구는 **문서 전체 수집**입니다. 현재 구조는 사용자가 지나가거나 편집한 문단을 telemetry로 받는 구조일 뿐, 문서 전체를 가진 구조가 아닙니다.

"세션 동안 지나간 문단만 누적"하는 방식은 빠른 프로토타입에는 가능하지만, 완전한 XLIFF나 완전한 번역 문서를 보장하지 못합니다. 사용자가 아직 방문하지 않은 문단이 빠지기 때문입니다. 정식 기능이라면 Word/InDesign 양쪽에 다음 중 하나가 필요합니다.

- 전체 문서 스캔 API
- 명시적으로 선택한 범위 스캔 API
- 스캔 결과의 문서 구조·순서·식별자 전달
- 스캔 이후 변경 감지와 stale 상태 관리

## "이중언어 저장 → XLIFF → 클린업"의 세 가지 해석

| 해석 | 산출물 | 규모/위험 | 권고 |
|---|---|---:|---|
| (a) 번역 세션을 XLIFF로 export | 문서와 독립된 sidecar XLIFF | 대 | 가장 먼저 |
| (b) XLIFF target만 이용해 새 번역 문서 생성 | 새 Word/InDesign/텍스트 문서 | 매우 대 | XLIFF export 이후 별도 |
| (c) 에디터 원본을 원문+번역 병기 후 원문 제거 | 원본 문서를 대량 편집 | 매우 대 이상 | 비권고 |

### (a) XLIFF sidecar export

가장 현실적인 첫 목표입니다.

장점:

- 원본 Word/InDesign 문서를 바꾸지 않는다.
- QA monolingual 구조를 유지한다.
- 번역 세션의 source/target·상태·TM 출처를 보관할 수 있다.
- 향후 CAT 교환을 위한 출발점이 된다.

한계:

- 문서 전체를 스캔하지 않으면 부분 XLIFF다.
- 텔레메트리 plain text만으로는 원래 문서의 서식·인라인 코드·레이아웃을 재현할 수 없다.
- external CAT에서 수정한 XLIFF를 다시 원문 문서에 반영하려면 별도의 import/merge 설계가 필요하다.

### (b) 번역문만 새 문서로 생성

이는 "XLIFF writer"의 다음 단계가 아니라 **문서 생성기**입니다.

최소한 필요합니다.

- 문서 전체의 문단 순서 및 계층
- 표, 머리글/바닥글, 각주, 텍스트 프레임 등 구조 정책
- Word/InDesign별 새 문서 생성 API
- 스타일 및 인라인 서식 재현 정책
- target 누락 문장의 정책
- XLIFF import 뒤 target이 바뀐 경우 매핑·충돌 처리

초기에는 "번역문만 줄바꿈으로 내보내는 plain text/Markdown" 정도가 가능하지만, 이를 Word/InDesign 번역 문서라고 부르면 안 됩니다. Word/InDesign 품질의 결과물을 약속하려면 별도 대형 프로젝트입니다.

### (c) 실제 에디터 문서를 이중언어로 만들고 클린업

가장 위험합니다.

- 원본 문서의 각 문장 옆에 target을 삽입해야 한다.
- 나중에 원문만 정확히 제거해야 한다.
- 표, 각주, 캡션, 인라인 서식, anchored object, 스토리/텍스트 프레임을 다뤄야 한다.
- 사용자가 중간에 수정·Undo·복사·재정렬한 경우 원문/번역 연결이 깨진다.
- 복구 실패가 단순 sidecar 오류가 아니라 원본 문서 손상으로 이어질 수 있다.

이는 과거 보류된 인접 삽입 기능보다 훨씬 위험한 대량 편집 버전입니다. 이 경로를 선택한다면 자동 치환과도 별개로, 문서 백업/스냅샷, 구조 태그, 호스트별 원자적 작업, 복구 실험을 선행해야 합니다.

## XLIFF 1.2와 2.0/2.1

### 권고

첫 외부 교환 포맷은 **제한된 plain-text XLIFF 1.2 export**로 시작하는 편이 현실적입니다. 단, "Trados/memoQ와의 호환"은 실제 대상 버전과 fixture를 받은 뒤 호환 테스트로 확정해야 합니다. XLIFF 버전만 맞는다고 모든 CAT 도구와 round-trip 호환이 보장되지는 않습니다.

XLIFF 1.2의 최소 구조는 보통 다음과 같습니다.

- `<xliff version="1.2">`
- `<file original=... source-language=... target-language=... datatype=...>`
- `<body>`
- 고유 `id`를 가진 `<trans-unit>`
- `<source>`
- 선택적 `<target>` 및 상태

XLIFF 1.2에서 `<trans-unit>`의 `id`는 필수이고, source/target은 동일한 file의 언어 속성과 일관되어야 합니다. [OASIS XLIFF 1.2 사양](https://docs.oasis-open.org/xliff/v1.2/cs02/xliff-core.html)

XLIFF 2.x는 모델이 더 명확합니다.

- `<xliff srcLang=... trgLang=...>`
- `<file>`
- `<unit>`
- `<segment>`
- `<source>`, 선택적 `<target>`

문장 단위 세그먼트를 중심으로 설계하기에는 XLIFF 2.x의 `unit/segment` 구조가 자연스럽습니다. 하지만 기존 CAT 도구와의 실무 상호운용성은 사용하는 제품과 버전, 필터 설정에 따라 검증해야 합니다. [OASIS XLIFF 2.1 사양](https://docs.oasis-open.org/xliff/xliff-core/v2.1/xliff-core-v2.1.html)

### 인라인 태그는 선행 조건

현재 TMX parser는 `<bpt>`, `<ept>`, `<ph>`, `<it>`, `<ut>` 등을 제거합니다. [`tmx_parser.rs`](/D:/smartLinter/src-tauri/src/tm/tmx_parser.rs:163)

따라서 현재 파서에서 읽어 보관한 TM 텍스트만으로는 태그를 보존한 XLIFF를 생성할 수 없습니다. 이미 확인된 실제 TMX 데이터에도 인라인 태그가 존재하므로, 이것은 장식 기능이 아니라 데이터 보존 요구입니다.

XLIFF에서는 source와 target 사이의 인라인 코드 정합성이 핵심입니다. XLIFF 1.2는 `<g>`, `<x/>`, `<bpt>`, `<ept>`, `<ph>` 등의 코드 모델을, 2.x는 `<ph>`, `<pc>`, `<sc>`, `<ec>` 등의 모델을 사용합니다. XLIFF 2.1은 inline code 무결성을 보존하도록 요구합니다. [XLIFF 1.2 inline elements](https://docs.oasis-open.org/xliff/v1.2/cs02/xliff-core.html) [XLIFF 2.1 core](https://docs.oasis-open.org/xliff/xliff-core/v2.1/xliff-core-v2.1.html)

그러므로 다음 순서가 맞습니다.

1. 매칭용 정규화 텍스트와 별개로 raw tagged IR을 보관한다.
2. source/target의 코드 ID와 짝 관계를 보존한다.
3. 태그가 포함된 fixture로 export/import round-trip을 검증한다.
4. 그 뒤에야 "CAT 호환 XLIFF"라고 말할 수 있다.

## 번역 모드 단계별 권고

| 단계 | 범위 | Release gate | Rollback/중단 기준 |
|---|---|---|---|
| T0. 요구사항 고정 | sidecar인지, 새 문서 생성인지, bilingual 편집인지 결정 | 산출물 정의와 지원 호스트 확정 | 범위 불명확하면 구현 착수 금지 |
| T1. 번역 세션 스파이크 | 선택 문단의 source/target을 세션에 누적 | 문장 ID·순서·stale 모델 확인 | 원본 문서 변경 없음 |
| T2. plain-text XLIFF export | 선택 범위 XLIFF 1.2 생성 | XML schema/fixture/CAT import 검사 | export 실패 시 세션 보존, 파일 미완성 표시 |
| T3. 문서 전체 스캔 | 문서 전체 또는 명시 범위 수집 | Word/InDesign 순서·누락·변경 감지 검증 | 문서가 바뀌면 export를 stale로 표시 |
| T4. 태그 보존 XLIFF | raw tagged IR과 코드 정합성 | 실제 TMX/Word/InDesign fixture round-trip | 태그 정합 불명 시 plain-text 모드로만 제한 |
| T5. XLIFF import/merge | 외부 CAT 수정본을 세션에 반영 | ID·source hash·상태 충돌 테스트 | 충돌은 자동 병합하지 않고 사용자 선택 |
| T6. 새 번역 문서 생성 | target 기반 Word/InDesign 생성 | 문서 구조·서식 fixture 검증 | 원본 문서에는 절대 쓰지 않음 |
| T7. 이중언어 편집/클린업 | 원본 문서 대량 편집 | 호스트별 원자성·복구·백업 검증 | 기본 비활성, 실패 시 즉시 중단 |

---

# 두 제안의 의존성과 로드맵

자동 치환과 번역 모드는 직접적인 선행 관계가 없습니다.

- 자동 치환은 현재 TM 기반 워크플로만으로 독립 실행할 수 있습니다.
- 번역 모드/XLIFF sidecar도 자동 치환 없이, 사용자가 TM 후보를 수동 채택하는 방식으로 시작할 수 있습니다.
- 다만 둘 다 문장 단위의 안정적인 ID·경계·부분 치환을 필요로 하므로, 문장/TU 경계 계약은 공통 선행 과제입니다.

권고 순서는 다음과 같습니다.

1. Stage 1c: 문장 경계 계약과 문장 단위 원클릭 적용 완성
2. TM 검색을 문단 단위에서 문장 단위로 전환
3. 자동 치환 A/B 단계: 미리보기와 수동 일괄 exact 적용
4. 번역 모드 T1/T2: 선택 범위 번역 세션과 plain-text XLIFF export
5. 태그 보존 IR 및 XLIFF 태그 round-trip
6. 자동 치환 D: 명시적 세션 자동 모드
7. 전체 문서 스캔과 XLIFF import/merge
8. 필요성이 검증된 경우에만 새 번역 문서 생성

SDLTM 지원은 TM 조회/저장 기능의 확장이고, XLIFF export와 직접 선행 관계는 없습니다. 다만 SDLTM에서 태그·컨텍스트를 보존해 가져올 계획이라면 tagged IR 설계와 함께 해야 합니다.

## 최종 권고

지금 당장 제품 우선순위를 정해야 한다면 다음 조합이 가장 안전하고 가치가 큽니다.

- 단기: 문장 단위 TM 검색/적용 + exact 후보 미리보기 + 수동 일괄 적용
- 중기: 번역 세션 저장소 + 선택 범위 XLIFF 1.2 sidecar export
- 보류: fuzzy 자동 적용, 문단 이탈 자동 적용, 에디터 내 bilingual 병기/cleanup, 원본 형식을 보존한 새 Word/InDesign 번역 문서 생성

이렇게 하면 Phase 0의 "에디터에 열린 문서가 원문이며 QA는 monolingual" 결정을 유지하면서도, 사용자가 원하는 CAT 성격의 번역 워크플로를 점진적으로 확보할 수 있습니다.
