# 기능 제안 검토: 전체 문서 실시간 QA 스캔

## 결론

제안의 문제의식은 타당하다. 활성 선택 문단만 보는 현재 방식은 문서 전체의 미처리 이슈를 발견할 수 없고, AI 커맨드로 이미 바뀐 문서와 기존 QA 카드의 상태가 어긋나는 상황도 사용자가 쉽게 마주친다.

다만 **문서 전체를 매 idle tick마다 재스캔하고 모든 문단을 LLM에 다시 보내는 형태의 상시 감시**는 권장하지 않는다. InDesign의 모든 Story/문단 열거, 문단 안정성 판정, 변경 추적, 큐 제어, 카드 정합성 처리가 새로 필요하며, 수십~수백 문단에서 Ollama 호출이 계속 누적되면 CPU·메모리·전력·응답성이 모두 나빠질 수 있다. 특히 InDesign의 idle 동작은 포커스 상실 시 멈출 수 있으므로, 이를 완전한 실시간 감시로 약속하는 것도 정확하지 않다.

권장하는 제품 정의는 다음이다.

> 포커스 문단은 즉시 검사하고, 문서 전체는 변경분만 유휴 시간에 점진적으로 검사하며, 사용자가 원할 때는 명시적으로 전체 검사를 시작·중단할 수 있다.

즉, 기존 `start_batch_scan`/`abort_batch_scan`을 폐기하지 말고 이 기능의 기반으로 확장하는 것이 적절하다.

## 1. 구조적 타당성 및 비용

현재 `onIdleTick → 활성 선택 문단 감지 → 1초 디바운스 → analyzeParagraph` 경로는 짧은 상호작용에 맞춰져 있다. 전체 문서 지원에는 다음 계층이 추가되어야 한다.

1. InDesign에서 문서·Story·문단을 안전하게 열거하는 스냅샷 단계
2. 문단별 지속 앵커와 정규화 텍스트 해시를 기록하는 변경 탐지 단계
3. 우선순위·동시성·취소를 갖는 분석 큐
4. 분석 결과가 어느 스캔 세대와 범위에서 나온 것인지 기록하는 카드 reconciliation 단계

문단을 순회하는 DOM 작업 자체도 큰 문서에서는 비용이 있지만, 더 큰 비용은 LLM 호출이다. 동일 문단을 매번 재분석하면 문단 수 × 모델 지연이 그대로 쌓인다. 로컬 Ollama는 네트워크 비용은 없더라도 CPU/GPU, RAM/VRAM, 팬·배터리, 다른 작업의 응답성을 사용한다. 단일 요청도 수 초 걸릴 수 있으므로, 수백 문단을 무제한 큐잉하면 사용자는 오래된 결과가 현재 문서에 뒤늦게 나타나는 문제를 겪게 된다.

따라서 전체 감시는 해시가 바뀐 문단과 아직 한 번도 스캔하지 않은 문단에 한정해야 한다. 분석 요청에는 문단의 `baseHash`와 스캔 세대(generation)를 포함하고, 응답을 반영하기 직전에 최신 해시·세대인지 확인해야 한다. 바뀐 문단의 옛 응답은 버린다. 문서 저장/닫기·에디터 연결 해제·모델 변경 시에도 큐를 취소 또는 재시작할 수 있어야 한다.

## 2. 일회성 전체 검사와 상시 감시의 관계

두 기능은 대체 관계가 아니라 목적이 다르므로 병행이 맞다.

| 기능 | 목적 | 동작 |
| --- | --- | --- |
| 포커스 즉시 검사 | 편집 중 빠른 피드백 | 현재 문단을 높은 우선순위로 분석 |
| 백그라운드 증분 검사 | 문서 전반의 최신성 유지 | 변경·미검사 문단만 유휴 시간에 조금씩 분석 |
| 전체 검사 / 중단 | 사용자가 원하는 명시적 완결성 | 현재 문서 스냅샷 전체를 저우선순위 큐에 넣고 진행률·취소 제공 |

상시 증분 감시는 앱이 꺼져 있던 동안의 변경, 관찰이 누락된 문단, 새로 열린 문서의 기존 본문을 즉시 보장하지 않는다. 반대로 전체 검사는 “이 시점의 문서 전체를 검사했다”는 명확한 사용자 계약과 진행률, 재시도, 중단 기능을 제공한다. 따라서 사용자가 요청한 기존 전체 검사 버튼은 유지해야 한다. 버튼은 전체 문서를 다시 검사하기보다, 가능하면 “이 스냅샷에서 아직 미검사이거나 변경된 모든 문단 검사”로 동작하게 해 불필요한 재호출을 피하는 편이 좋다. 별도 ‘강제 전체 재검사’가 필요하다면 그 뜻을 UI에 명시해야 한다.

## 3. 권장 절충 설계

첫 구현은 아래의 제한된 증분 스캐너가 현실적이다.

1. 문서가 유휴 상태일 때만 짧게 문단 목록을 순회한다. 한 tick에서 전체를 훑지 말고 일정 개수 또는 짧은 시간 예산만 사용한다.
2. 각 문단은 문서 식별자, Story 식별자, 지속 앵커(가능하면 label/UID, 불가하면 문맥 fingerprint 포함), 정규화 해시, 마지막 분석 상태를 가진다. 위치 기반 `paragraphId`만으로 지속성을 판단하지 않는다.
3. 새 문단과 해시 변경 문단을 큐에 넣고, 이미 같은 해시로 PASS/분석 완료된 문단은 건너뛴다. 모델·가이드라인·TM 버전이 바뀌면 해당 버전을 캐시 키에 포함해 필요한 범위만 무효화한다.
4. 우선순위는 현재 선택/방금 편집한 문단, 사용자 요청 전체 검사, 오래 기다린 변경 문단, 최초 미검사 문단 순으로 둔다. LLM 동시성은 보수적으로 1부터 시작하고, 큐 길이·평균 지연·Ollama 오류율을 관측한 뒤 조절한다.
5. 사용자가 타이핑 중이거나 InDesign이 바쁘면 새 LLM 요청을 시작하지 않고 잠시 양보한다. 중단은 아직 시작하지 않은 큐 항목을 즉시 제거하고, 이미 진행 중인 요청 결과는 세대 검증 후 무시한다.
6. UI에는 ‘문서 검사 중’, 진행률, 대기열 수, 마지막 완전 스캔 시각, 중단 버튼을 보여 준다. 백그라운드 검사는 기본적으로 조용해야 하며, 결과 카드가 한꺼번에 쏟아질 때의 묶음/필터 UX도 필요하다.

이 설계에서 전체 스캔 결과는 카드 정합성의 권위 있는 입력이 될 수 있다. 다만 스캔 범위 안에서 동일한 지속 앵커의 최신 결과가 PASS일 때만 pending 카드를 완료/무효 처리해야 한다. 앵커가 사라졌거나 여러 후보로 모호하면 자동 삭제하지 말고 `stale_obsolete` 또는 재확인 필요 상태로 보관해야 한다. 부분 스캔 결과로 범위 밖의 카드를 정리하면 안 된다.

## 4. Task F와 AI 커맨드로 인한 카드 불일치

Task F는 이번 화면의 증상을 **조건부로** 완화한다. 새 분석 결과의 문단 텍스트에 기존 `originalSegment`가 없고 `suggestedSegment`가 있으며, 같은 Story에서 그 조건을 만족하는 pending 카드가 정확히 하나일 경우 해당 카드를 정리한다. 따라서 AI 커맨드가 문서를 수정한 뒤 daemon이 그 문단의 새 텔레메트리를 보내고, 이어서 clean QA 결과가 도착한다면 ‘일오일 → 일요일’ 카드가 정리될 가능성이 높다.

그러나 Task F만으로 AI 커맨드와 자동 QA 파이프라인이 완전히 연동되는 것은 아니다.

- Task F는 후속 텔레메트리와 분석이 실제로 발생·성공한다는 전제에 의존한다. 포커스 이동, idle 중단, 디바운스 취소, 분석 실패면 정리가 일어나지 않을 수 있다.
- 같은 Story에 같은 교정 패턴이 둘 이상이면 안전장치상 자동 정리를 하지 않는다. 이는 올바른 안전 동작이지만 사용자가 여전히 stale 카드를 볼 수 있음을 뜻한다.
- AI 커맨드의 치환 성공 이벤트가 어떤 문단·해시를 바꿨는지 QA 카드 수명주기에 명시적으로 전달되지 않는다. 따라서 이 경로는 결정적 연동이 아니라 관측 기반 사후 정리다.

Task F는 예정대로 먼저 완료·실검증하는 것이 맞다. 그 뒤 장기적으로는 AI 커맨드 적용 결과에도 변경된 문단 앵커와 적용 후 해시를 포함시키고, 해당 문단을 높은 우선순위로 재분석하거나 검증된 카드 상태 전이를 발생시키는 연결이 필요하다. 단, AI가 생성한 텍스트가 기존 제안과 정확히 같은지 확인되지 않은 경우에는 자동 ‘해결’ 처리 대신 재분석을 우선해야 한다.

## 5. 우선순위 제안

전체 문서 스캔은 가치가 크지만 현재 포커스 기반 경로와 카드 수명주기 자체가 막 안정화되는 단계이므로, 곧바로 ‘상시 실시간’ 기능부터 구현하는 것은 위험하다. 권장 순서는 다음과 같다.

1. Task F 완료 및 실제 InDesign에서 AI 커맨드 적용·직접 수정·문단 이동 시나리오를 검증한다. `stale` 카드는 적용 불가 상태로 안전하게 전환되어야 한다.
2. 위치 보기와 지속 앵커/문단 재식별 기반을 안정화한다. 이는 스캔 결과와 카드의 올바른 연결에도 필요하다.
3. `start_batch_scan`/`abort_batch_scan`을 먼저 설계·구현한다. 문단 열거, 큐, 진행률, 취소, 해시 캐시, 범위 기반 reconciliation을 여기서 검증한다.
4. 일괄 적용은 안정적인 문단 앵커와 최신 해시 검증 뒤에 진행한다. 전체 스캔 결과와 결합될 때 오적용 위험이 커지므로 스캔 기반보다 앞서면 안 된다.
5. 일회성 스캔의 관측치와 성능 예산이 확인된 뒤에만, 같은 엔진을 저속의 백그라운드 증분 감시로 확장한다. 기본값은 보수적으로 두고 사용자가 끌 수 있어야 한다.
6. 적용 전 인라인 수정과 완료 카드 아카이브는 스캔 엔진과 독립적으로 사용자 효용이 있어 병렬 후보가 될 수 있지만, 위치 보기/Task F 검증을 앞지르지는 않는 편이 안전하다.

요약하면, 이 제안은 `전체 문서 일괄 스캔` 백로그를 대체하지 않고 그 위에 놓이는 확장 요구다. 먼저 취소 가능한 일회성·증분 스캔을 신뢰성 있게 만들고, 성능과 카드 정합성 데이터가 확보된 뒤 상시 감시를 활성화하는 단계적 접근을 권장한다.

본 문서는 검토 의견이며 코드 변경이나 구현 지시는 포함하지 않는다.
