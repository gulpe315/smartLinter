# Task: QA 카드 생명주기 정합성 — Step 5 (F5 차단 + 영속화 + 복원 시 재검증)

`DESIGN_QA_CARD_LIVE_INTEGRITY.md`의 **Part 5**와 Suggested implementation
order 5번입니다. Step 1~4(단일/배치 라이브 스냅샷, 새 카드 게이팅, Layer 1
수동 무효화, Layer 2 JIT 뷰포트/포커스 검증+오프라인/재연결)까지 전부
완료·커밋됐습니다(`0909ec5`→`ccf20a8`→`7ae3d28`→`d7598e2`). 재설계/재자문
불필요 — 설계 문서 그대로 구현하면 됩니다. 이번 단계는 앞 단계들과 대체로
독립적입니다.

## 배경 (설계 문서 Part 5 원문 요약)

`useQaStore`가 순수 인메모리 Zustand 상태라 F5를 누르면 QA 카드가 전부
날아갑니다(Rust 백엔드/InDesign COM 연결은 살아있고 프론트엔드 JS 상태만
날아감). 현재 `beforeunload` 리스너도, F5 차단도, `persist` 미들웨어도
전혀 없습니다(확인됨). 두 모델 다 "경고 팝업만으로 막기"(브라우저식
"사이트를 나가시겠습니까" 다이얼로그)를 데스크톱 앱 안티패턴으로 명시
배제하고, 아래 조합(D)에 합의했습니다.

## 구현할 것

### 1. 프로덕션에서 F5/새로고침 차단

최상위(`App.tsx` 또는 진입점)에 `keydown` 리스너를 달아 `F5`,
`Ctrl+R`/`Cmd+R`, `Ctrl+Shift+R`/`Cmd+Shift+R`를 `preventDefault()`로
막으세요. WebView2의 기본 우클릭 메뉴(새로고침/검사 항목)도 비활성화하는
게 이상적이지만, 최소한 우클릭 메뉴 자체의 새로고침 동작이 실제로
막히는지 확인하고, WebView2 API로 메뉴 항목 자체를 못 지우면 최소한
`contextmenu` 이벤트에서 전체 기본 메뉴를 막는 것으로 대체해도 됩니다
(VS Code/Slack/Figma 같은 데스크톱 셸 앱들의 관례 — F5는 이런 앱에서
사용자 기능이 아닙니다).

**개발 환경(`npx tauri dev`, Vite HMR)에서는 이 차단을 끄세요** —
`import.meta.env.DEV`로 분기(Vite 표준 전역, 이 프로젝트에 아직 이
패턴을 쓰는 곳이 없으니 새로 도입해도 됩니다). HMR로 인한 잦은 전체
리로드(의존성 변경, 에러 복구)가 개발 중 QA 카드를 계속 날려서 반복
작업 속도를 해치면 안 되고, 오히려 dev에서는 F5가 영속화 경로 자체를
수동으로 검증하는 유용한 도구가 됩니다.

### 2. 명시적 "다시 스캔" / "상태 초기화" UI 액션

헤더나 설정 패널에 명시적 버튼을 추가하세요(정확한 위치는 기존 UI
구조와 자연스럽게 어울리는 곳으로 판단 — `Header.tsx`의 기존 버튼들이나
설정 모달을 참고). 클릭 시 `cards`/`dismissedCards`/`appliedCards`를
초기화하는 확인 동작이면 됩니다(영속화가 되므로 데이터 손실 우려 없이
제공 가능 — 확인 다이얼로그는 선택사항, 굳이 필요 없다고 판단하면
생략해도 됩니다).

### 3. `useQaStore`에 영속화 추가

`cards`/`dismissedCards`/`appliedCards`를 최소한 `localStorage`에
저장하세요. Zustand의 공식 `persist` 미들�웨어 사용을 권장합니다(설계
문서가 명시한 메커니즘). **참고:** `configStore.ts`는 `persist`
미들웨어가 아니라 필드별 수동 `localStorage.getItem`/`setItem`
(`STORAGE_KEYS` 상수) 패턴을 씁니다 — 설계 문서가 "configStore의 기존
패턴을 참고하라"고 한 건 이 저장 위치/네이밍 관례를 말하는 것이지 반드시
같은 수동 방식을 강제하는 게 아닙니다. `persist` 미들웨어를 쓰는 게 이번
요구사항(스키마 버전, 여러 필드 묶어서 직렬화)에 더 적합하다고 판단되면
그렇게 하고, `configStore`와 저장소 키 네이밍 스타일만 맞추면 됩니다.

### 4. 영속화 데이터는 항상 "복원 후보"일 뿐, 신뢰의 원천이 아님

저장할 때 카드 데이터와 함께 다음도 같이 저장하세요:
- 문서 식별자 — `useBridgeStore`의 `activeDocument`
- 연결/세션 식별자 — `useBridgeStore`의 `sessionId`
- 타임스탬프 + 스키마 버전(향후 마이그레이션 대비 정수 버전 필드)

**하이드레이션(앱 시작/새로고침 복원) 시:**
1. **저렴한 사전 필터, COM 호출 전:** 저장된 문서 식별자가 현재 연결된
   문서와 다르거나(또는 InDesign이 아예 연결 안 돼있으면), 카드를
   "활성"으로 복원하지 마세요 — 그냥 폐기하거나(가장 단순) 최소한
   보이지 않는 상태로 두세요. 정확한 처리(완전 폐기 vs 숨김 보관)는
   기존 `dismissedCards`/`stale_obsolete` 패턴과 자연스럽게 어울리는
   쪽으로 판단하세요.
2. **일치하면:** 복원된 카드는 즉시 "활성"으로 보여주지 말고
   `restoring`/검증대기 상태로 시작해서, **Step 4에서 만든 것과 정확히
   같은 라이브 스냅샷 게이트**(`validateLiveCards` 또는 그 내부 로직)를
   통과해야만 유효한 카드로 표시되게 하세요. 새 게이트를 따로 만들지
   말고 기존 `validateLiveCards`를 하이드레이션 직후(에디터 연결
   확인되는 시점)에 호출하는 식으로 재사용하세요.
3. `beforeunload` 리스너도 보조 안전망으로 추가하되(주 방어선이 아님 —
   플랫폼마다 확인 UX가 다르고 사용자가 그냥 진행을 눌러버릴 수 있음),
   너무 비중 두지 마세요. 카드 생성과 영속화 완료 사이 짧은 틈만
   메우는 역할입니다.

## 하지 말 것 (범위 이탈 방지)

- Step 1~4에서 만든 기존 라이브 검증 로직(`getLiveParagraphSnapshot(s)`,
  `validateLiveCards`)의 판정 규칙 자체를 바꾸지 마세요 — 이번 단계는
  그걸 하이드레이션 시점에도 재사용하는 것뿐입니다.
- `configStore.ts`의 기존 수동 localStorage 패턴 자체를 리팩터링하지
  마세요(참고만 하고 건드리지 않음).
- ExtendScript/Rust 변경 없음 — 이번 단계는 순수 프론트엔드입니다.

## 테스트

- `src/stores/__tests__/qaStore.test.ts`: 영속화 라운드트립(저장→재로드
  후 필드 복원), 문서 식별자 불일치 시 복원 안 됨, 일치 시 복원되지만
  `restoring` 상태로 시작해서 라이브 게이트 통과 전엔 유효 카드로 안
  보이는지.
- 컴포넌트/키보드 테스트(`npm run test:ui`): 프로덕션 모드에서 F5 keydown
  이벤트의 기본 동작이 막히는지(`preventDefault` 호출 확인), dev 모드
  플래그일 때는 안 막히는지. "다시 스캔" 버튼 클릭 시 카드가 초기화되는지.

## 완료 후

`cargo test`, `npm test`, `npm run test:ui`, `npm run build` 전부 통과해야
합니다. 순수 프론트엔드 변경이라 브릿지 서버 재기동 불필요. 이번 단계도
사용자 지시로 실제 InDesign 라이브 검증(실제 F5를 눌러서 카드가 살아남는지,
문서를 바꿔서 복원이 거부되는지)은 생략하고 자동 테스트 통과 후 바로
커밋합니다(추후 필요 시 사용자가 별도로 라이브 확인 예정). **이걸로
`DESIGN_QA_CARD_LIVE_INTEGRITY.md`의 Step 1~5 전부 완료됩니다.**
