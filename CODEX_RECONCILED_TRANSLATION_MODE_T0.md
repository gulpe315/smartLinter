## 최종 권장안

### 1. 세션 진입: telemetry 기반 자동 Upsert 채택

번역 모드가 켜진 동안 현재 선택/포커스된 `activeParagraph`를 세션에 자동 Upsert하는 편이 T1에 더 적합합니다. T1의 목적은 문장 ID·순서·stale 모델을 실제 편집기 telemetry와 연결해 검증하는 것이므로, 문단마다 별도 "세션에 추가" 동작을 요구하면 스파이크의 관찰 흐름 자체가 불필요하게 왜곡됩니다.

Codex 안의 "방문하지 않은 문단이 빠진다"는 지적은 사실이지만, 이는 T1의 실패가 아닙니다. T1은 "사용자가 번역 모드에서 관찰한 문단"을 다루는 스파이크이고, 전체 문서/범위의 완전한 수집은 T3의 스캔 기능이 책임져야 합니다. 인용된 완전성 경고도 T3/XLIFF 수준의 완전한 산출물을 만들 때 적용하는 것이 맞습니다.

다만 자동 편입은 다음의 명확한 경계가 필요합니다.

- 번역 모드 ON 상태에서만 수집한다.
- 문서 식별자와 문단 ID를 키로 멱등 Upsert한다.
- "세션에 들어온 문단"이 곧 "문서 전체"라는 의미가 아님을 상태/UI에 명시한다.
- 사용자가 모드 OFF 또는 세션 종료를 선택하면 더 이상 수집하지 않는다.

즉, 자동 수집을 채택하되 T1 세션을 "방문/선택 기반 세션"으로 정확히 정의해야 합니다.

### 2. target 초기값: 100% Exact-유일 TM만 자동 채움, 단 미확정 초안으로 보관

`eligible`인 100% Exact-유일 매치에 한해 target 초안을 자동 채우는 안을 권장합니다. T1이 단순 저장 구조를 넘어서 TM 결과와 세션 데이터 모델의 상호작용을 검증하려면, 실제로 `deriveTmAutoApplyPlan`의 eligible 경로를 통과시키는 편이 가치가 있습니다.

그러나 이는 "번역 확정"이 아닙니다. 다음처럼 모델링해야 합니다.

- `targetDraft = candidate.target`
- `origin = 'tm-exact'`
- `isUserEdited = false`
- `status = 'suggested'` 또는 동등한 미확정 상태
- 자동 export·자동 적용·AI 후속 처리 금지

Fuzzy, conflict, 매치 없음은 target을 비워 둡니다. Codex 안처럼 후보와 출처만 기록하는 방식은 가장 보수적이지만, "100% 유일 TM 결과를 세션 초안으로 표현할 수 있는가"를 T1에서 검증하지 못해 데이터 모델 스파이크의 범위를 지나치게 축소합니다. 반대로 agy 안도 `target`이 곧 확정 번역이라는 오해를 막는 상태 필드를 명시하지 않으면 불충분합니다.

### 3. 세션 수명: T1부터 영속화, 복구 시 전 세그먼트 `needs-validation` 처리

Codex 안을 채택합니다. T1부터 세션을 영속화해야 하며, 앱 재시작 후 복구된 모든 세그먼트는 일괄적으로 `stale/needs-validation`으로 표시해야 합니다. 복구 직후에는 자동 export, 자동 적용, 마지막 캡처 해시의 신뢰를 허용하지 않습니다.

agy의 stale 충돌 우려는 타당하지만, 제시된 Codex 방어책이 정확히 그 위험을 제거합니다. 외부에서 Word/InDesign 문서가 바뀌었더라도, 복구 데이터는 신뢰 가능한 현재 문서 상태가 아니라 검증 대기 중인 작업 초안일 뿐입니다. 따라서 오래된 source/target을 자동으로 내보내거나 문서에 적용해 사고를 내는 경로가 없습니다.

반면 인메모리 전용은 다음 손실을 감수하게 합니다.

- 앱 재시작·충돌·실수로 닫은 경우 작업 세션이 전부 사라진다.
- T1이 검증하려는 세션 식별자·해시·TM 출처·stale 전이 모델 중 "재시작 후 복구"를 검증할 수 없다.
- T2의 `.xliff`가 완성될 때까지 사용자는 복구 가능한 작업 단위를 갖지 못한다.

`.xliff`는 교환/산출 포맷이지, 진행 중인 세션의 복구 저장소를 대체하지 않습니다. 저장 위치와 포맷을 T0에서 고정하지 않아도 되지만, "저장 가능하고 안전하게 복구 가능하다"는 동작 계약은 T1 수용 조건이어야 합니다.

## 이전 결론 대비

- Codex의 기존 입장과 비교하면 쟁점 3의 결론은 바뀌지 않습니다.
- agy의 인메모리 전용 결론은 바뀌어야 합니다. 결정적인 이유는 stale 위험이 영속화 자체의 문제가 아니라, 복구 데이터를 현재 문서와 동등하게 신뢰하는 정책의 문제이기 때문입니다. 전량 `needs-validation` 복구 정책이 그 위험을 차단합니다.
- 쟁점 1의 완전성 경고는 T1 자동 수집을 금지하는 근거가 아니라 T3 전체/범위 스캔에 요구되는 조건입니다.
