# Task: TM 패널 라운드3 수정 — 잔여 결함 8건

라운드2 수정에 대해 Codex와 agy가 각각 독립 리뷰한 결과 잔여 결함이
확인됐다. 두 리뷰는 상충하지 않으며 서로 다른 것을 잡았다.

**범위 제약 동일**: `plugins/`, `src-tauri/` 절대 수정 금지. `src/`만.

## 결함 1 — searchMode가 keyword에서 돌아오지 않음 (최우선, 기능 무력화)
`src/stores/tmStore.ts`의 `initEventListener`에서 `new-paragraph-detected`
핸들러가 `currentParagraph`와 `searchQuery`는 갱신하지만 `searchMode`는
그대로 둔다. 따라서 **사용자가 키워드 검색을 한 번이라도 쓰면 이후 정상
문단 작업에서도 [TM 저장]이 영구히 비활성화**된다. 기능이 고장난 것처럼
보인다.

수정: 자동 문단 검색이 발생하면 `searchMode`를 `fuzzy`로 되돌릴 것.
(또는 모드 대신 `현재 검색이 자동 문단 검색에서 왔는가`를 별도 상태로
두고 그것으로 판정할 것. 어느 쪽이든 위 증상이 사라져야 한다.)

## 결함 2 — 다문장 문단 저장을 차단이 아니라 경고로 완화 (사용자 확정)
사용자가 명시적으로 결정함: **경고 후 저장 허용.**
짧은 2문장 문단도 실무에서는 하나의 TU로 등록하는 경우가 흔하므로
완전 차단은 과하다.

수정: `isSingleSentence`를 `canSaveToTm`의 **비활성화 조건에서 제거**할 것.
대신 문단이 복수 문장일 때는 저장 직전 확인 대화상자에 경고 문구를
추가할 것 — 예: 문단에 문장이 여러 개입니다. 문단 전체가 하나의 TM
항목으로 저장됩니다.
`searchMode`/`searchQuery`/문단 존재/공백 타깃 조건은 그대로 비활성화
조건으로 유지한다.

## 결함 3 — 문장 판정에 말줄임표 누락
실제 `KO-EN.tmx`를 직접 측정한 결과:
- `。`(U+3002): **20,885개 TU 중 0건.** 한국어에 안 쓰인다.
  **지시서에서 제외한다. 넣지 말 것.**
- `…`(U+2026): 192건 중 **51건이 세그먼트 중간에서 `… ` 형태**로 나타나며
  실제 문장 경계다. 실측 용례:
  `이전 메시지를 불러오는 중… 시간이 조금 지체될 수 있습니다.`
  `동기화 중입니다. 잠시 기다리세요… 동기화하는 데 많은 시간이 소요될 수 있습니다.`

수정: `sentenceCount`의 종결부호에 `…`를 추가할 것(`。`는 추가하지 말 것).
기존의 `뒤에 공백 또는 문자열 끝` 조건은 유지한다.
결함 2로 이제 이 판정은 차단이 아니라 **경고 문구를 띄울지 결정**하는 데
쓰인다.

## 결함 4 — 편집됨 시각 표시 누락 (라운드2 미이행)
라운드2 지시서에 명시했으나 구현되지 않았다. 편집 확정 후 표시는
편집본으로 바뀌지만, 그것이 원본 TM 제안인지 사용자가 수정한 것인지
구별할 방법이 없다.

수정: 편집된 상태(`editedSuggestion !== null`)일 때 `수정됨` 뱃지를
표시할 것.

## 결함 5 — 검색어 비교가 공백 차이에 취약
`searchQuery === currentParagraphText` 엄격 비교라 trim/개행 정규화
차이만으로 저장이 잠긴다.

수정: 양쪽 `.trim()` 비교로 완화할 것.

## 결함 6 — 충돌 시 기존 번역을 안 보여주고 덮어씀
같은 원문이 이미 TM에 있을 때 확인 대화상자가 충돌 사실만 알리고
**기존에 저장돼 있던 번역이 무엇인지 보여주지 않는다.** 사용자가
무엇을 덮어쓰는지 모른 채 확인하게 된다.

수정: 확인 대화상자에 기존 번역 전문을 함께 표시할 것.

## 결함 7 — React key 성능 회귀
`src/components/tm/TMMatchPanel.tsx:329`의 key에 `candidates.map(...)`로
**후보 목록 전체**를 직렬화해 넣었다. 후보 하나만 바뀌어도 모든 카드가
unmount/remount된다.

수정: key는 카드 고유 식별에 필요한 최소한만 쓸 것(문단 식별자 +
후보 식별자 정도). 문단이 바뀔 때 편집 상태가 초기화되는 요구사항은
유지하되, 후보 목록 전체를 key에 넣는 방식은 쓰지 말 것.

## 결함 8 — 테스트 보강
아래를 추가할 것.
- `수정됨` 뱃지가 편집 시 나타나고 취소 시 사라지는지.
- `…`로 이어진 복수 문장 문단에서 확인 대화상자에 **경고 문구가 포함**되는지
  (위 실측 용례 문장을 그대로 쓸 것).
- 단일 문장일 때는 경고 문구가 **없는지**.
- 확인 대화상자에서 취소했을 때 저장이 실제로 중단되는지.
- 충돌이 있을 때 확인 대화상자에 **기존 번역 전문이 포함**되는지.
- 키워드 모드로 갔다가 새 문단 이벤트가 오면 [TM 저장]이 **다시 활성화**되는지
  (결함 1 회귀 방지).
- `searchQuery`와 문단 텍스트가 공백만 다를 때 저장이 활성화되는지.

## 완료 조건
`npm test`, `npx vitest run`, `npm run build` 전부 통과.
