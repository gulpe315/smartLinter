# [재조율 답변] 번역 모드 T0 설계 상충 3건 최종 권장안

`RECONCILE_TRANSLATION_MODE_T0.md`에서 제시된 3개 상충 쟁점을 면밀히 재검토하고, 상대 주장의 기술적 타당성과 SmartLinter의 현재 아키텍처 제약 및 T1/T2 로드맵을 종합적으로 평가하여 각 쟁점별 최종 권장안과 명확한 논거를 보고합니다.

## 📊 3대 쟁점 최종 결론 요약

| 쟁점 | 최종 권장안 | 채택 방향 | 핵심 결정 사유 |
| :--- | :--- | :---: | :--- |
| **쟁점 1: 세션 진입/멤버십** | **번역 모드 ON 상태의 Telemetry 자동 Upsert (내부 API는 명시적 단일 진입점)** | **agy 안 채택** | T1 스파이크는 사용자 인터랙션 마찰 없는 세그먼트 누적·stale 검증이 목적. Codex의 "불완전성"은 T3(전체 스캔)의 과제이지 T1 수집을 가로막는 근거가 아님. |
| **쟁점 2: target 초기값** | **TM 100% Exact-유일 매치만 `targetDraft`에 Pre-fill (`origin: 'tm-exact'`, `status: 'draft'`)** | **agy 안 채택** | T1에 UI/AI 입력이 없는 상황에서 target을 비워두면 T2 XLIFF export 검증이 불가능함. 에디터 문서 수정이 아닌 사이드카 초안이므로 안전함. |
| **쟁점 3: 세션 수명·영속성** | **영속성 필수 (단, 세션 복구 시 무조건 `stale/needs-validation` fail-closed 정책 강제)** | **입장 전환(Codex 방어책 수용)** | 복구 시 전면 stale 마킹과 baseHash 검증을 통해 agy가 우려했던 "외부 문서 변경 오염 위험"이 100% 방어됨. 번역 세션은 보존 가치가 높은 실질 작업물임. |

## 쟁점 1: 세션 진입/멤버십 모델 — 자동 편입 vs 명시적 추가

### 최종 권장안
번역 모드 ON 상태에서의 Telemetry 기반 "포커스 문단 자동 Upsert" 채택. 단, 세션 스토어의 내부 구현은 `upsertParagraphSegments(paragraph)` 형태의 명시적 단일 진입점 메서드로 캡슐화한다.

### 논거 및 상대 주장(Codex 안) 반박
1. **T1 스파이크의 본질과 "불완전성" 경고의 오적용 반박:** Codex가 인용한 `CODEX_ANSWER_AUTO_TRANSLATE_AND_TRANSLATION_MODE.md:226-228`의 "방문 문단 누적은 완전성을 보장하지 못한다"는 경고는 정식 제품 단계인 T3(전체/범위 스캔 API)에서 완전한 문서를 생성하기 위한 요구사항이다. T1의 목표는 "문장 ID 부여, 세그먼트 순서 관리, stale 모델 확인"을 검증하는 경량 스파이크다. 아직 Range Selection API나 Batch Scan API가 없는 T1 단계에서 "완전성이 없으므로 자동 수집을 금지하고 매번 수동 추가 버튼을 누르게 한다"는 것은 검증 속도를 불필요하게 떨어뜨리고 사용자 마찰을 극대화한다.
2. **인터랙션 마찰 제거와 일관성:** 번역 모드가 활성화된 동안 수신되는 `activeParagraph`를 세션 스토어가 자동으로 문장 세그먼트로 분할·누적하는 것이 가장 자연스럽다. 문단 순서 왜곡은 세그먼트 메타데이터에 `paragraphId` 및 문서 내 출현 순서를 보존해 데이터 모델 단계에서 충분히 제어할 수 있다.

## 쟁점 2: target 초기값 — TM 자동 채움 vs 항상 빈 값

### 최종 권장안
TM 100% Exact-유일 매치(`eligible`)에 한해 `targetDraft` 사전 채움(Pre-fill) + `origin: 'tm-exact'`, `status: 'draft'` 메타데이터 기록. 충돌/Fuzzy/매치 없음은 `targetDraft: ''`, `origin: 'empty'`, `status: 'untranslated'`로 초기화.

### 논거 및 상대 주장(Codex 안) 반박
1. **T1/T2 파이프라인 연속성 보장(핵심):** T1은 전용 편집 UI나 AI 연동이 없는 데이터 모델 스파이크다. 만약 Codex 안처럼 모든 세그먼트의 `targetDraft`를 빈 값으로 고정한다면, 곧바로 이어지는 T2(plain-text XLIFF export) 단계에서 `<target>` 태그에 유의미한 데이터가 채워지는지 엔드투엔드로 검증할 방법이 없어진다.
2. **에디터 문서 치환(위험)과 사이드카 초안 채움(안전)의 명확한 구분:** Codex의 우려는 "사용자 검수 없는 자동 확정의 위험"에 기인한다. 그러나 트랙 B와 달리 번역 모드는 사이드카 세션 스토어 내부의 독립된 작업 공간이다. `deriveTmAutoApplyPlan` 로직을 재사용해 100% Exact이자 유일한 매치인 경우에만 초안으로 pre-fill하고 `origin: 'tm-exact'`를 남기는 것은 CAT 도구의 표준적인 사전 번역 동작이며 원본 문서를 전혀 훼손하지 않는다.

## 쟁점 3(가장 중요): 세션 수명 — 인메모리 전용 vs 영속 필수

### 최종 권장안
T1부터 번역 세션의 영속화(Persistence)를 지원하되, 복구 시 무조건 `stale/needs-validation`으로 복원하는 Fail-Closed 복구 정책 채택. 저장소 구현체는 Zustand `persist` 미들웨어 또는 로컬 스토리지/IndexedDB 어댑터 활용, 상세 포맷은 T0 범위 밖.

### "짚어줄 점" 심층 검토 및 입장 전환(철회)의 결정적 계기
1. **"짚어줄 점" 반박의 타당성 인정:** agy 원안에서 "인메모리 전용, 재시작 시 소멸"을 주장했던 유일한 기술적 이유는 "앱 재시작 후 사용자가 Word/InDesign 외부에서 문서를 수정하거나 다른 문서를 열었을 때, 과거 영속된 세그먼트 데이터가 에디터 상태와 충돌하여 발생할 수 있는 데이터 오염/불일치 위험"이었다. 그러나 Codex가 제시한 "세션 복구 시 이전 캡처 해시를 신뢰하여 자동 적용/export하지 않고, 복구된 모든 세그먼트를 무조건 `needs-validation` 상태로 초기화하여 재검증을 강제하는 정책"은 agy가 우려했던 시나리오를 기술적으로 완벽하게 차단한다.
2. **결정적 전환 계기(작업 산출물의 본질적 가치):** 트랙 B Stage C의 되돌리기 로그는 단기 1회성 세션 이력이므로 인메모리 소멸이 타당했다. 하지만 번역 세션은 사용자가 장시간에 걸쳐 수집하고 검수한 문장 쌍, TM 출처, 초안 텍스트가 축적되는 핵심 작업 산출물이다. 안전 장치(`needs-validation` 복원 + `baseHash` 일치 검증)가 마련된 이상, 비정상 종료나 앱 재시작 시 사용자의 작업물이 통째로 증발하는 인메모리 정책을 고수할 이유가 없다. 따라서 agy는 이전의 인메모리 고집을 철회하고, Codex의 "Fail-Closed 영속성 복구 모델"을 전적으로 수용하여 최종 권장안으로 확정한다.
