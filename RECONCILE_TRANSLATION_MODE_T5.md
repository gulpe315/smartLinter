# 재조율 요청 — 트랙 C: 번역 모드+XLIFF T5(XLIFF import/merge)

`DESIGN_REQUEST_TRANSLATION_MODE_T5.md`에 대한 두 자문의 답변
(`AGY_ANSWER_TRANSLATION_MODE_T5.md`, `CODEX_ANSWER_TRANSLATION_MODE_T5.md`)
이 왔다. 질문 1(매칭은 `segmentId` 완전 일치만, 느슨한 재매칭 금지),
질문 2의 "진짜 충돌" 기본 정의(`isUserEdited: true` + 다른 텍스트),
질문 3(XLIFF `state`를 `needs-validation`으로 강등하지 않고 `draft`로
신뢰 수용 — `needs-validation`은 원문 스냅샷 불신 전용이지 번역 품질
승인 여부가 아니라는 논리), 질문 4(import는 세그먼트 단위 fail-closed
+ 부분 성공, T2의 export 전체 차단과는 성격이 다름), 질문 6(UI는
`Header.tsx` 기존 클러스터 재사용, 결과 요약 배너)은 처음부터 사실상
수렴했다. 아래 세 지점만 재조율이 필요하다.

## 참고 — 재조율 대상이 아닌, Codex의 추가 안전장치로 그대로 채택하는 사항

Codex가 질문 1 답변에서 추가로 제시한 두 가지는 agy 답변과 상충하지
않는 순수 추가 방어책이라 재조율 없이 그대로 채택한다: ① `segmentId`
일치와 별개로 `<source>` 텍스트도 현재 세션의 `sourceText`와 정확히
일치하는지 재확인(ID만 믿지 않는 2차 무결성 검증), ② import 파일
내부에 동일 `id`가 두 번 이상 등장하면 그 항목들은 매칭하지 않는다.
(참고로만 공유, 답변 불필요.)

## 쟁점 1 — `tool-id="SmartLinter"` 헤더 마커를 필수로 요구할 것인가

이건 정면으로 갈렸다.

- **agy**: "필수로 요구하지 않습니다(관대하게 수용)" — Trados/memoQ/
  Phrase 등 주요 CAT 툴이 export 시 `<header>`를 자사 메타데이터로
  교체하거나 생략하는 경우가 흔해서, `tool-id` 하드 체크는 "실제
  CAT 도구를 거쳐 온 정상 파일"을 거부하는 치명적 호환성 결함이라고
  주장. 대신 `segmentId`(+source) 일치 여부만으로 안전성을 담보하면
  충분하다는 입장.
- **Codex**: "표준 XLIFF 1.2라고 해서 관대하게 받아들이지 않는 편이
  맞다" — T5를 "임의의 XLIFF reader"가 아니라 "SmartLinter XLIFF
  round-trip importer"로 명확히 규정하고, `<header>` 안에
  `tool-id="SmartLinter"`인 `<tool>`을 **필수 요구사항**으로
  명시(§5 요구사항 목록). 외부에서 새로 만든 XLIFF는 `segmentId`
  계약이나 세션 source snapshot 보장이 없어 우연한 ID 충돌 위험이
  있다는 논리.

이 쟁점이 실제로 중요한 이유: T5의 목적 자체가 "외부 CAT 툴에서
수정된 파일을 다시 가져오는 것"인데, 만약 실제 CAT 툴들이 `tool-id`
헤더를 정말로 자주 덮어쓰거나 제거한다면, Codex 안을 그대로 채택할
경우 **T5가 실사용에서 정작 필요한 시나리오(진짜 CAT 툴 왕복)에서
파일을 거부하게 되는 자기모순**이 생길 수 있다. 반대로 agy 안대로
관대하게 받아들이면 Codex가 우려하는 "우연한 세그멘테이션 방식이
다른 임의 XLIFF" 유입 위험이 남는다. 두 자문 모두 실제 Trados/memoQ/
Phrase의 XLIFF export가 커스텀 헤더 속성(`tool-id` 등)을 얼마나
보존/파괴하는지에 대한 구체적 근거(문서/경험)를 갖고 있는지 재확인하고,
절충안(예: `tool-id`가 있으면 신뢰도 표시에 참고만 하고, 없어도
`segmentId`+`source` 이중 검증이 통과하면 받아들이되 "SmartLinter가
직접 만들지 않은 파일일 수 있음" 경고만 보여주는 식)이 가능한지도
검토해달라.

## 쟁점 2 — import 직전 "문서 전체 재스캔"을 필수 선행 조건으로 둘 것인가

- **Codex**: 명시적으로 필수 단계로 규정(§4) — "검증은 파일을 고른
  직후의 오래된 persist 세션이 아니라, import 직전에 수행한 성공적인
  전체 문서 스캔 결과를 기준으로 해야 한다." 근거: 세션이 앱 재시작
  등으로 복원된 오래된 상태일 수 있고, 그 저장된 `sourceHash`가
  실제 라이브 문서와 이미 어긋나 있을 수 있다 — `segmentId` 매칭이
  "세션에 저장된 값"과만 일치를 확인할 뿐 "지금 실제 문서"와의 일치는
  보장 못 한다는 논리. InDesign의 경우 미배치 스토리 옵트인 여부가
  아직 확정 안 된 상태에서 import를 시작하면 안 된다는 구체적 사례도
  듦.
- **agy**: 이 요구사항을 전혀 언급하지 않음 — import 흐름에 "먼저
  전체 재스캔부터 강제"하는 단계가 없다.
- Codex의 우려가 실제로 유효한지(즉 "세션에 저장된 sourceHash"와
  "라이브 문서의 실제 원문"이 어긋날 수 있는 현실적 시나리오가
  있는지 — 예: 앱을 껐다 켰는데 그 사이 Word/InDesign에서 문서가
  수정된 경우, 세션은 `onRehydrateStorage`로 전부 `needs-validation`
  처리되긴 하지만 T5 import가 그 상태를 어떻게 다루는지가 애초에
  질문 3~4에서 명확히 다뤄지지 않았음) agy가 재검토하고, "import 시작
  전 전체 재스캔을 강제할지" 여부와 "그러면 InDesign 미배치 스토리
  옵트인처럼 이미 있는 다른 강제 조건과 UX가 어떻게 얽히는지"를
  명확히 답해달라.

## 쟁점 3 — `isUserEdited: true`인데 외부 XLIFF의 target이 빈 문자열인 경우

- **agy**: "무충돌"로 분류하고 **자동으로** 기존 사용자 편집본을
  보존한다(사용자에게 묻지 않고 조용히 무시).
- **Codex**: "같은 변경이 로컬 사용자 수정본을 지우려 하면 충돌이다"
  — 즉 이 경우도 다른 충돌과 동일하게 세그먼트별 선택 UI에 올려서
  사용자가 명시적으로 확인하게 해야 한다는 입장(빈 값이라고 자동으로
  무시하면, 외부 CAT 검토자가 "이 문장은 삭제/재작업 필요"라고
  일부러 비워서 돌려줬을 가능성을 사용자가 못 보게 될 수 있음).
- 어느 쪽이 맞는지 판단해달라 — "사용자 편집본을 외부의 빈 값으로
  조용히 삭제하지 않는다"는 안전 원칙 자체는 둘 다 동의하는 것으로
  보이는데, 그 처리를 "자동으로 안전하게 유지"(agy, UI 노이즈 감소)
  로 할지 "충돌로 표시해 사용자가 직접 확인"(Codex, 투명성 우선)으로
  할지가 갈린다. 이 프로젝트가 지금까지 반복적으로 "안전 vs 투명성"
  중 어느 쪽으로 기울어왔는지(예: T2/T3 전반의 fail-closed 경향)를
  참고해서 판단해달라.

## 답변 형식

`{CODEX|AGY}_RECONCILED_TRANSLATION_MODE_T5.md`로, 쟁점 1~3에 대한
결론과 근거를 응답 텍스트로 직접 출력해달라(파일 저장 지시 없음).
