# Word 위치찾기 미지원 + 문장 단위 카드/하이라이트 정밀도 설계 자문

사용자가 Word 라이브 사용 중 발견한 3가지를 한 번에 자문 요청합니다.
아직 아무것도 구현/합의 안 된 상태 — 코드 수정은 하지 말고 분석/설계
의견만 부탁합니다.

## 1. [확정 버그] "위치 보기"가 Word 세션에서도 InDesign만 찾음
`src-tauri/src/commands.rs:343-362`의 `locate_paragraph_in_editor`가
`get_live_paragraph_snapshot`(오늘 Step1/2에서 고친 것)과 똑같은
패턴으로 `session.editor_type != EditorType::InDesign`이면 무조건
`"Locate paragraph is supported only for InDesign"` 에러를 반환합니다.
Word에서 [위치 보기]를 누르면 사용자에게 "InDesign 연결 상태를 확인할
수 없습니다"로 잘못 표시됨(스크린샷 확인).

**차이점 — 오늘 만든 snapshot과 다른 점:** snapshot은 비침습적
읽기전용 조회였지만, `locate_paragraph`는 실제로 InDesign에서 해당
문단을 **선택/스크롤**(포커스 이동)시키는 액션입니다. Word Office.js로
"커서를 안 뺏고 위치만 조회"하는 것과, "실제로 그 문단으로 스크롤/
선택 이동시키는 것"은 다른 API 필요(`range.select()`,
`range.scrollIntoView()`류로 추정). Word 쪽엔 아직 이런 API 지원이
전혀 배선 안 돼 있음(Step1/2는 read-only 스냅샷만 만들었음).

**질문:** 오늘 만든 `LIVE_SNAPSHOT_REQUEST`류 왕복 RPC 인프라를
재사용해서 `LOCATE_REQUEST`/`LOCATE_RESPONSE` 같은 새 메시지타입을
추가하는 게 맞는지, 설계와 구현 범위를 스코핑해주세요.

## 2. [설계 질문, 사용자 제안] 문단을 문장 단위로 끊어서 분석해야 하는가?
현재 구조: 문단 전체를 LLM에 보내고, LLM(+결정론적 검사)이 그 문단
안에서 여러 `QaIssue`를 찾으면 **각 이슈마다 별도 QA 카드**가 생성됨.
사용자가 실사용 중 관찰한 문제: 한 문장 안에 문제가 여러 개(예:
구두점+어미)면 카드가 여러 장 뜨는데, 사용자 기대는 "한 문장 = 카드
하나(그 안에 이슈 여러 개 표시)"에 더 가까움.

**사용자 제안:** 마침표/구두점 등으로 문장을 먼저 분리한 뒤, 문장 단위로
카드를 만들고 카드 하나 안에서 여러 유형의 문제를 표시하는 게 낫지
않은가?

**참고 — 관련 기존 인프라:**
- `QaIssue`에 이미 `start_offset`/`end_offset`(UTF-16 코드유닛 기준
  문단 내 위치) 필드가 있음(`src-tauri/src/ai/qa_parser.rs:141-146`,
  결정론적 사전 병합 작업 때 추가됨). **그런데 프론트엔드
  (`src/`) 어디에서도 이 필드를 전혀 안 씀** — grep으로 확인, 죽은
  데이터임.
- `conflict_group_id`(부분겹침 연결요소, union-find 병합용)도 이미
  있음 — 이것도 "여러 이슈를 하나로 묶는" 것과 관련 있을 수 있음.
- 이 프로젝트는 이미 "동일 이슈 일괄 적용"(`getNormalizedIssueKey`
  기반 그룹핑) 기능이 있음 — 이건 "같은 문제가 여러 문단에 반복"일
  때 묶는 거라 이번 요청(한 문장 안의 서로 다른 문제 유형을 하나의
  카드로)과는 다른 축.

**질문:**
1. 문장 분리(마침표/느낌표/물음표 등 구두점 기준) 자체가 안전한가?
   한국어 문장 경계 판단의 함정(예: "박사님.", 인용부호 안 마침표,
   줄임표 등)이 이 프로젝트가 이미 겪은 조사호응 화이트리스트류
   함정과 비슷한 리스크가 있는지.
2. "문장 단위 카드 + 카드 내 다중 이슈 표시"로 바꾸면 영향 범위가
   얼마나 큰지(QaCardData 스키마, qaStore의 카드 생성/dedup/stale
   판정 로직, UI 컴포넌트 전부 연쇄 영향 가능성 큼 — 정확한 스코프
   산정 부탁).
3. 지금 당장 전면 재설계할 사안인지, 아니면 더 작은 개선(예: 같은
   문장 범위를 침범하는 이슈들만 UI에서 시각적으로 그룹핑, 실제 카드
   스키마는 안 바꾸는 절충안)으로 충분한지 의견 부탁.

## 3. [설계 질문, 사용자 제안] 하이라이트를 전체 문장이 아니라 문제 구간만
사용자 제안 원문: "유형별로 카드를 다 발생시키겠다고 한다면 수정해야
할 부분에만 하이라이트가 필요할 것 같아. 현재는 위치 찾기를 하면 모든
문장을 하이라이트하고 있어."

즉 **2번 질문과 별개로**(카드를 문장단위로 합치든 안 합치든), 위치
찾기/하이라이트 자체는 문단·문장 전체가 아니라 `originalSegment`가
가리키는 정확한 구간만 강조해야 한다는 제안.

**질문:**
1. 이미 있는 `start_offset`/`end_offset`이 이 목적에 바로 쓸 수 있는
   데이터인지 확인 부탁(LLM 경로가 실제로 이 필드를 채우고 있는지,
   아니면 결정론적 검사 경로만 채우고 LLM 경로는 비어있는지 코드로
   확인).
2. InDesign COM/Word Office.js 양쪽에서 "문단 전체가 아니라 특정
   오프셋 구간만" 선택/하이라이트하는 게 기술적으로 가능한지(현재
   `locate_paragraph`/`atomic_replacer.jsx`가 문단 단위로만 동작하는
   것으로 보이는데, 오프셋 기반 부분 선택 지원이 이미 있는지 없는지
   확인).
3. 이번 세션에 InDesign도 아직 이 방식으로 안 바뀌어 있을 텐데, Word만
   먼저 정밀 하이라이트를 지원하는 게 일관성 문제를 만들지 않는지.

## 우선순위 관련 참고
1번(버그)은 명확한 버그라 스코핑되면 바로 착수 가능. 2번/3번은 상당한
설계 변경이라 사용자가 우선순위/착수 여부를 별도로 결정할 것 — 지금은
분석/설계 의견만 받고 실제 착수는 보류.
