# 자동 치환(자동 번역) 기능 + [번역 모드](CAT 툴화 + XLIFF) 설계 자문

## 사용자 제안 원문 그대로

> 1. 에디터+대시보드 연결 상태에서 TM 불러오고, 현재 에디터의 커서가 위치한 문장을 매치하여
>    그 결과를 보여줄거아. 그것을 우리가 치환하느냐 복사하느냐이고(검색 기능은 일단 논외)..
>    이 상황에서 하나씩하나씩 수동으로 작업하는 것 외에 미리 자동번역(즉 자동 치환)하는
>    기능도 적용할 수 있을까?
> 2. 이미 TM을 불러올 수 있으므로, 대시보드에 [번역 모드] 기능을 추가해서 번역 모드로
>    전환하면 Trados 등 CAT Tool로 변신하는 건 어때? 이것을 이중언어로 저장하면 xliff
>    파일을 만들고, 클린업하면 번역 문서가 추출되는 것으로.

**코드 수정은 하지 말고 설계 분석만 부탁합니다.** 두 제안 모두 이번 자문에서 처음 나온
것이고 아직 사용자가 우선순위/스코프를 확정하지 않았습니다 — 각 제안의 실행 가능성,
리스크, 단계별 쪼개기를 제시해 주시면 사용자가 그걸 보고 결정합니다.

## 반드시 먼저 반영해야 할 기존 결정 (재검토 요청이 아니라, 이 제안들과의 관계를 짚어달라는 것)

`PHASE0_SOURCE_DATA_CONTRACT_FINDINGS.md`(§2, §4.3)에서 사용자가 이미 명시적으로 확정한
내용이 있습니다:

> 워드, 인디자인 등 에디터에 열린 문서들이 원문이야. 문서를 xliff 파일처럼 언어쌍이 있는
> 문서를 대상으로 하는 것은 아니야.

그 결정으로 Codex가 그때 권고했던 "XLIFF/SDLXLIFF를 canonical contract로 삼는 Phase 1 ADR"
은 채택되지 않았고, `qaStore`가 `source`를 항상 빈 문자열로 고정하는 monolingual 파이프라인이
그대로 유지되고 있습니다(QA=모노링구얼 린터, TM=별도 지식베이스, 이 둘은 병렬 기능).

**이번 제안 2번(번역 모드 + XLIFF 저장)이 그 결정과 정면으로 부딪히는지, 아니면 공존
가능한지**를 명확히 짚어주세요. 예상되는 두 갈래:
- (a) "번역 모드"가 내부 데이터 계약(`ParagraphPayload.source`, QA monolingual 전제)을
  다시 bilingual로 바꾸자는 것이라면 — Phase 0 결정을 뒤집는 것이므로 그 자체로 별도의
  큰 재설계이자 리스크임을 명시해야 함.
- (b) "번역 모드"가 내부 데이터 계약은 그대로 두고, TM 매치/사용자 입력을 바탕으로 XLIFF는
  **파생 산출물로 사후 생성**하는 것이라면 — Phase 0 결정과 공존 가능할 수 있음.
어느 쪽이 사용자의 실제 의도에 더 가까울지, 그리고 기술적으로 어느 쪽이 더 안전한지 판단해
주세요.

## 현재 코드 상태 (직접 확인한 사실)

- TM 매칭은 **문단 단위**다. `src/components/tm/TMMatchPanel.tsx`가
  `useBridgeStore().activeParagraph`(에디터가 통보한 현재 문단)를 기준으로
  `useTmStore().search()`를 호출하고, Rust `fuzzy_matcher.rs`가 그 문단 텍스트 전체를
  TM `source`와 비교해 후보를 낸다. **"커서가 위치한 문장"을 짚어 매칭하는 기능은 아직
  없다** — 에디터가 커서 위치를 대시보드에 보내는 경로 자체가 없음(코드 검색으로 확인,
  "cursor" 관련 텔레메트리 없음). 다만 Stage 1a/1b(최근 커밋 `8e567d8`/`a932dc3`)에서
  Rust `segmenter::segment_sentences`/TS `splitIntoSentences`로 문단을 문장 단위로
  쪼개는 공용 계약이 이미 만들어져 있고, `[TM 저장]`이 이걸로 문장쌍 분리 저장을 이미
  하고 있다 — 문장 단위 조회로 확장할 때 재사용 가능한 기반이다.
- 현재 사용자 액션은 `TMMatchCard.tsx`의 `[적용]`(치환, `onApply` → `applyMatch`,
  해시검증 기반 `sendReplacementCommand`로 에디터에 반영), `[복사]`(클립보드),
  `[TM 저장]`(치환 없이 TM에만 반영) 세 가지이며 전부 **사용자가 카드를 보고 수동으로
  누르는 동작**이다. 문서를 자동으로/사용자 확인 없이 바꾸는 경로는 이 프로젝트에 아직
  전혀 없다.
- 이 프로젝트는 문서를 자동/프로그램적으로 건드리는 것에 대해 이미 한 번 신중하게
  분석하고 보류한 전례가 있다(`SmartLinter_Plan.md`의 "[붙여넣기](인접 삽입)" 기능,
  2026-08-29 세션에 사용자 결정으로 폐기). 그 분석에서 나온 위험 두 가지가 이번 제안
  1번(자동 치환)에도 그대로 적용될 수 있어 참고 바람:
  - **삽입/치환된 텍스트가 에디터 텔레메트리를 타고 다시 입력으로 되먹임된다**(새
    문단이 QA 린터와 TM 매칭에 마치 사용자가 방금 쓴 원문처럼 들어감) — 자동 치환도
    동일한 문제를 갖는다. 별도 차단/구분 장치가 필요.
  - Word taskpane은 Shared Runtime이라 배관 변경 시 재기동 함정이 있다(이번 제안과
    직접 관련은 적지만 구현 시 유의사항으로 남겨둠).
- `src-tauri/src/tm/tmx_parser.rs`가 TMX 파싱만 하고 **어떤 방향으로도 export를 만드는
  코드는 이 프로젝트에 전혀 없다**(SVG/PDF/웹매뉴얼 export는 있지만 번역 파일 포맷 export는
  없음). XLIFF writer는 100% 신규 구현이 필요하다.

## 질문 1: 자동 치환(자동 번역) 기능

- TM 매치를 사용자 확인 없이 자동으로 적용하는 것을, **어떤 조건**(100% exact match만?
  일정 스코어 이상 fuzzy도?)에서, **어떤 트리거**(문단 벗어날 때? 저장 시점? 명시적
  "자동 모드 켜기" 토글?)로 하는 게 안전한지 설계해 주세요.
- 자동 치환이 "[붙여넣기]" 기능이 폐기되며 남긴 두 가지 우려(텔레메트리 피드백 루프,
  해시 기반 트랜잭션과의 상호작용)를 어떻게 피해가야 하는지 구체적으로.
- 사용자가 원치 않는 자동 치환을 즉시 되돌릴 수 있는 UX(예: 자동 적용된 항목을 별도로
  표시하고 일괄 취소하는 기능)가 필요한지, 필요하다면 어떤 형태가 기존 롤백/해시검증
  메커니즘(`qaStore.ts`의 `acceptCard`/`processReplacementResult` 패턴, `TMMatchCard.tsx`
  의 `applyMatch`)과 가장 잘 맞는지.
- 이 기능이 "사람이 매 건 확인 후 적용"이라는 지금까지의 제품 전체 철학(QA 카드도,
  TM 매치도 전부 수동 확인 후 적용)과 충돌하는 지점이 있는지 짚어주세요.
- 단계별로 쪼갠다면 어떤 순서가 안전한지(예: 1단계는 "자동 적용 후보 미리보기만 표시,
  실제 치환은 안 함"처럼 관찰 전용 스파이크부터 시작).

## 질문 2: [번역 모드] — CAT 툴 전환 + XLIFF 저장/클린업

- "번역 모드로 전환"이 UX적으로/데이터적으로 무엇을 의미해야 하는지 설계해 주세요.
  현재 대시보드는 문서 자체를 들고 있지 않고(에디터가 진실의 원천), 문단이 지나갈
  때마다 텔레메트리로 받아 QA/TM에 흘려보내는 구조입니다. "번역 모드"가 되려면
  최소한 무엇이 새로 필요한지(예: 대시보드가 문서 전체의 문장별 원문↔번역 페어를
  세션 동안 누적하는 상태 저장소?).
- **"이중언어로 저장 → XLIFF" / "클린업 → 번역 문서 추출"**이 정확히 무슨 산출물을
  뜻하는지 몇 가지 해석이 가능한데, 각각의 구현 난이도/리스크를 비교해 주세요:
  (a) XLIFF는 대시보드가 세션 중 누적한 원문↔번역 페어를 XLIFF 1.2/2.0 규격으로
  내보내는 것 (문서 자체와는 독립된 사이드카 파일),
  (b) "클린업"이 XLIFF에서 번역문만 뽑아 새 문서(Word/InDesign 등)로 만들어내는 것,
  (c) 아니면 실제 에디터 문서 자체를 이중언어(원문+번역 나란히)로 만들었다가 클린업
  단계에서 원문을 제거하는 것(이러면 문서를 프로그램적으로 대량 편집해야 함 — 리스크가
  전혀 다른 차원).
- XLIFF 포맷 자체의 실사 부탁(버전 1.2 vs 2.0, 인라인 태그 처리 — 이건 Stage 1의 향후
  단계로 예정된 "인라인 태그 보존"과 정확히 맞물림, Trados/memoQ 등 실제 CAT 툴이
  요구하는 필수 필드/속성).
- 이 제안이 위 "먼저 반영해야 할 기존 결정" 절에서 물은 (a)/(b) 갈림길 중 어느 쪽으로
  가야 Phase 0 결정과 최소 충돌하는지 결론을 내려주세요.
- 이게 얼마나 큰 작업인지(Stage 1b까지 진행된 "문장/TU 경계 계약" 로드맵과 비교했을
  때 상대적 규모), 그리고 지금 로드맵(Stage 1c 문장 원클릭 적용, TM 검색 문장단위 전환,
  SDLTM, 인라인 태그 보존)과 어떤 순서로 배치하는 게 합리적인지 권고해 주세요 — 완전히
  새로운 트랙으로 병행 가능한지, 아니면 기존 로드맵의 특정 단계가 선행돼야 하는지.

## 공통 요청 사항

- 두 제안 모두 "지금 당장 얼마나 크고 리스크 있는 작업인지"를 최우선으로 평가해 주세요.
  사용자가 우선순위를 정할 수 있도록, 각 제안을 여러 단계/스파이크로 쪼개고 단계별
  release gate/rollback 기준을 제시해 주세요(이 프로젝트의 다른 큰 설계들과 동일한
  형식).
- 두 제안이 서로 의존 관계가 있는지(예: 번역 모드가 먼저 있어야 자동 치환이 의미가
  있는지, 아니면 독립적으로 각각 가능한지)도 판단해 주세요.
- 기존 로드맵(문장/TU 경계 계약 Stage 1c 이후, TM 검색 문장단위 전환, SDLTM, 인라인
  태그 보존)과 충돌하거나 순서를 바꿔야 하는 지점이 있다면 명시해 주세요.
